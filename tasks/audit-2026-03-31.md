# TwinMe Audit — 2026-03-31

## Summary
| Severity | Count |
|----------|-------|
| CRITICAL | 2 |
| HIGH | 8 |
| MEDIUM | 11 |
| LOW | 8 |
| **Total** | **29** |

---

## CRITICAL

- [x] C1. **test-evidence-pipeline route exposed in production** — No auth, no dev guard. Anyone can trigger evidence pipeline for any userId via GET /api/test-evidence-pipeline/:userId.

- [x] C2. **Stack trace leak on malformed JSON** — errorHandler.js falls through to generic handler that includes stack. Verify NODE_ENV=production is set on Vercel.

---

## HIGH

- [x] H1. **76 routes leak error.message** — beta-admin.js, eval.js, etc. return raw error.message without NODE_ENV check.
- [x] H2. **19 cron jobs** — Two at */15, two at */30, one hourly. Code comments say "every 5 minutes" but vercel.json says */15.
- [x] H3. **maxDuration: 120s globally** — CLAUDE.md says 60s. Doubling GB-hour cost.
- [x] H4. **cron-token-refresh.js dead file** — Not in vercel.json. Should be deleted.
- [x] H5. **Uniform rate limiting 1000/15min** — /api/twin/chat (LLM calls) same limit as health checks.
- [x] H6. **JWT errors leak details** — Returns "jwt malformed" and "invalid signature".
- [x] H7. **path-to-regexp HIGH vuln** — ReDoS. Fix via npm audit fix.
- [x] H8. **/api/admin returns 401 not 404** — Reveals admin surface to unauthenticated users.

---

## MEDIUM

- [x] M1. /api/health takes 500-1000ms — DB check included. Should be <100ms.
- [x] M2. deliver-insights comment says "5 min" but config says */15.
- [x] M3. prospective-check comment says "5 min" but config says */15.
- [ ] M4. Landing page no gradient orbs visible — flat dark bg.
- [ ] M5. Auth left panel no glass card — flat dark.
- [ ] M6. Google button gray/brown — should be #F5F5F4 per design system.
- [ ] M7. Mobile auth no glass wrapper.
- [x] M8. 404 handler leaks route list in dev.
- [ ] M9. Two competing error handler middlewares.
- [x] M10. yaml moderate vuln — fix via npm audit fix.
- [ ] M11. Soul signature layers still 29.4s (from March 25 audit).

---

## LOW

- [ ] L1. Landing "Services" nav — off-brand.
- [ ] L2. Large whitespace below hero CTA.
- [ ] L3. Auth "Welcome back" shown to new users.
- [ ] L4. Rate limit headers expose remaining count.
- [ ] L5. Footer "TwinMe Inc." — verify entity name.
- [ ] L6. GET /api/auth/refresh returns 404 not 405.
- [ ] L7. Carryover: /discover footer dead links.
- [ ] L8. Carryover: Demo mode fires 9x 401.

---

## TOP 5 ACTION ITEMS

1. Remove or auth-gate test-evidence-pipeline (C1)
2. Verify NODE_ENV=production on Vercel + fix stack trace leak (C2)
3. Reduce maxDuration 120s to 60s (H3)
4. npm audit fix for path-to-regexp (H7)
5. Sanitize error.message in 76 route handlers (H1)
