# TwinMe Money — the program, and where it stands

This file is the one place both agents (Claude Code and Codex) and Stefano look to know what is being done, by whom, and what is next. It is tracked in git on purpose: `.claude/plans/` is gitignored and the July agentic-OS plan was lost that way.

## How to use this file — the protocol for every agent

1. **Read it first**, every session, before touching code.
2. **Claim before you start.** Change a task's status to `doing (your name, date)`. If another agent has claimed it, work on something else or leave a note under Decisions.
3. **Finish with evidence.** `done (#PR, date)` and one line of what was measured. A test that fails without the change is the default evidence.
4. **Never silently undo the other agent.** Reverting a change or rewriting a test's assertions requires a Decisions entry saying why, with the measurement. (#419: one agent rewrote the other's balance tests to assert the opposite; both were wrong, and the third version — settle by the bank's own reading — was the one that shipped.)
5. **One source of truth for instructions.** `CLAUDE.md` is canonical; `AGENTS.md` must be byte-identical (`npm run sync:agents`, checked in CI). On 2026-09-19 `AGENTS.md` was a 170-line-drifted copy that still named a design system retired on 12 September — which is how #417 shipped off-register.
6. **Branches:** `claude/<topic>` and `codex/<topic>`. Plans that matter go here or in `docs/`, never in a gitignored directory.
7. **Money is the product.** Anything touching `money_*` tables or the bank is never automated on a test streak (`autonomous_writes:false` in the report card stays hard-coded).

Status words: `todo` · `doing (who, date)` · `done (#PR, date)` · `blocked (why)` · `dropped (why)`.

## Owner directives (2026-09-19, verbatim intent)

- **Money first.** TwinMe Money is the first product. Everything else is secondary. A signed-in user lands on `/money`; the twin comes after.
- **Design:** orbs, chat and loading use the exact design of https://libraries.dev/orbs (Jakub Antalik's thinking-orbs).
- **Work with Codex** in both directions.
- **Check whether the legacy twin is used** before parking it.
- **Claude chooses where the loop runs.**
- **Begin with the small wins**, then prioritise the rest.
- **Keep scouting** for ideas, competitors, papers, articles, X — creative, innovative, robust, a wow.
- **The standard:** complete, tested, documented; the finished product, not a plan.

- **2026-09-24:** "forget Spotify and the other platforms connected in TwinMe; money is the new product we are making from scratch; use the platforms we have in the money platform and that make sense to our product." The sources are the bank (PSD2), the phone's notifications, the receipts address, statements, the calendar, WhatsApp. The legacy OAuth platforms are parked (D20) and go with the parked API on 2026-10-22 (M2-B). "If we have a WhatsApp agent it should be inspired in Instinct too": the reverse-engineering of 2026-09-21 and the walkthrough of 2026-09-23 are the reference; the first take is reactions as answers (M3-13).

## Now

### Verified release checkpoint — 2026-09-25, after #592

- **done (#592, Codex):** ambiguous observations are deferred once, explicit review resolves atomically, and dependent forecast/chat/export/scoring paths withhold guidance until evidence is clear. Merged as `d3dad8248`; normal Git deployment `dpl_AQyCm1JAK7KNaaN9urwEtFdXa8PW` READY with both twinme.me aliases. Final CI run36143925425 and secret scan36143925434 passed. No historical financial repair.
- **Schema verified:** live history records20260925134843 (`money_deferred_sightings`) and20260925134910 (`money_revision_conflicts`), corresponding to repository creation-time files20260925125543 and20260925132839. MCP uses application-time versions; do not infer absence or replay by filename alone. Before/after276 payments and397 observations retain identical full-row fingerprints (new null reconciliation column excluded).
- **Conflict repair verified over real HTTP:** stale scoring/ingestion requests return409/PT409 in865/996ms; no ledger writes. Database dashboard became Healthy. Preserve PT409 on rollback; restoring custom40001 business exceptions reintroduces internal retry loops.
- **Checks:** recorded local runs include4,702 root tests,1,559 Money coverage tests (76.66% lines/62.53% branches),77 real PostgreSQL checks,72 browser checks and32 native behavior tests. Final CI completed browser teardown cleanly; an earlier local assertion-complete run needed teardown interruption. The native test typing failure was fixed without weakening gates.
- **Live:** authenticated Today reports clear/0 pending; Ask composer works. Month/Plan/You/Account phone navigation has no failing Money requests, page errors or horizontal overflow. A separately intercepted pending-review scenario hid guidance and required explicit confirmation; zero real financial writes. Browser intercepts were removed.
- **Devices:** actual Android35 and iOS26.5 release-screen fixture checks passed review/retry/error states. Earlier iOS input-tool blocker did not recur. Audit client restored afterward; owner package untouched. Native source is merged, but no store/binary/OTA distribution is claimed.
- **doing (Codex):** fresh screenshot-only design iterations remain local. Round51 phone7.3, round52 phone7.0; round53 in progress. Requested9/10 gate remains open. Financial release does not imply visual approval.
- **doing (Codex):** narrow spreadsheet-feed approval/schema guard on `codex/money-sheet-approval-guard`. Real helper reproduction shows inserted Budget column imports100 rather than10 under the old mapping. Bind explicit approval to header structure, canonical source, account and interpretation; no route, cron or network exposure. Stable row updates/deletions and safe fetching remain separate rollout blockers.

Everything below is dated history unless a later checkpoint explicitly retains it as open.

### Deferred evidence release — 2026-09-25

- **Production incident fixed (13:32 UTC):** Business revision conflicts raised SQLSTATE `40001`, which PostgREST 14 retries internally with the same stale arguments. Logs confirmed millions of scoring retries. The two installed RPCs now raise `PT409`, preserving their bodies, owner, grants and invoker security; no ledger rows changed. Fresh activity showed no remaining scoring loops, so no sessions were terminated. The 13:32:30–13:35 UTC log window contained zero PostgreSQL errors. The application accepts both codes during rollout and retries only after a fresh snapshot, at most three times. Real PostgreSQL tests exercise the returned code through a bounded model of PostgREST's retry behavior; this is not a locally hosted PostgREST test.
- **Recovery verified:** A single production snapshot at 13:30:10 UTC restored all 27 Money tables / 6,101 rows with 69 constraints and complete-row fingerprints matching. A second native dump/restore passed the repository rehearsal (27 counts + five ledger hashes). Hosted physical backup dated 25 September 01:27 UTC also confirmed. Private artifacts stay outside git. This proves Money recovery, not restoration of Supabase Auth, Storage or the entire project.
- **Deployment drift found:** Production lacked repository merchant-key promotion and account-mask fallback. This slice includes the former through the new ingestion function. Account-mask fallback remains separately unshipped and needs collision review before application; no historical account merging is authorized by this finding.
- **Native truth states:** Unavailable or mismatched financial revisions now show a retry state, not a claim that payment review exists. Pending evidence has an explicit review action. Native handler/state tests retain real module helpers; they do not certify a distributed binary.

- **Contract fixtures:** Page/client mocks now carry explicit clear reconciliation status; unknown/missing status withholds guidance. Existing cache owner-isolation and single-read assertions remain unchanged; new failure-refresh test proves cached guidance disappears when completeness becomes unavailable. Incomplete financial schemas do not get coerced into zero. Page-read and one-read goal tests add the shared reconciliation read while preserving ledger/facts read counts.

- **Decision (Codex):** Replace the preparatory classifier test that deliberately pinned the old ambiguous attachment with a deferred/null expectation now that persistence/review/gates are being implemented together. Preserve all unambiguous/named/provider identity assertions. Design calculation disclosure tests may open the native details element before asserting the same exact server values; no financial value assertion is removed.

- **doing (Codex, 2026-09-25):** User authorized continuing and releasing remaining fixes. Implement F03 as a coordinated deferred-evidence slice: atomic storage and explicit review, conservative financial guidance and scoring, web/native recovery. Preserve existing financial assertions; no historical repair. Run local restore rehearsal and verify a fresh recoverable production backup before schema rollout. Design continues independently with fresh screenshot-only critics; the 9/10 gate remains open.

### Verified release checkpoint — 2026-09-25, 12:36 UTC

- **done (#585, Codex):** unfamiliar statement clarification and preview confirmation, retained upload drafts, bounded model samples and strict numeric column indices. Production `5a5ac318`, Git deployment `dpl_22koQ2v9e6BuNRH8614VA59u36WE` READY. Deployed phone flow verified with browser-intercepted requests only; no live import.
- **done (#587, Codex):** incomplete calendar reads preserve learned evidence; known Google reauthorization remains an error with a recoverable action. Web/native retain prior calendar on failure. Production `ad1547a99`, Git deployment `dpl_7yAcPcoBCrD34TPQSXXYxLwTa4G3` READY. Real calendar read200; simulated502 showed retry and returned to real200; known reauthorization showed reconnect. Health200; phone overflow absent. All interceptions removed. No calendar URL changed or financial repair performed.
- **Final measurements:** 4,690 root tests /11 skipped,70 desktop/phone browser tests,25 native tests, strict Money/mobile types and build; all CI gates passed. Native source changes are not a new distributed binary. Current iOS render works, but input-tool failure still blocks renewed interaction certification.
- **doing (Codex):** Ask design remains uncommitted and undeployed. Independent rounds36/37 did not meet9/10; selected round35 (7.0desktop/7.2phone) for complete computed prose and interaction clarity, preserving all alternate studies privately. Integrate released history protections before any future visual release; do not confuse test success with visual approval.
- **Next, in order:** ambiguity-safe reconciliation with deferred evidence; atomic source removal including derived copies; honest account/calendar coverage and refresh; durable WhatsApp execution/delivery. Calendar read integrity does not repair earlier unknown/zero history, source deletion, recurrence/timezone behavior or calendar-only scheduling. No new GPS collection for the student beta; calendar is context, not causal or payment evidence.

Older dated checkpoints below retain the work history; this checkpoint is the current release disposition.

### Calendar read integrity — 2026-09-25

- **done (#587, Codex, 2026-09-25):** A failed ICS source currently becomes an empty successful calendar read, replacing cached evidence and stamping a fresh `learned_at`. Reproduce before repair; fail incomplete aggregate reads, retain prior learned data, and report freshness only after successful persistence. Keep this slice separate from historical coverage, source-deletion migration and calendar-only scheduling. Decision: replace any tests asserting a failed feed is equivalent to a quiet diary; a failed source is unknown, not zero events. Work on `codex/money-calendar-read-integrity`. Source-screen copy now distinguishes calendar context from payment proof, removes the unverified daily-refresh promise, and names both Blackboard Ultra and older link routes (web EN/ES/PT and native). Copy-only edits use existing type/build/browser checks rather than mirrored string tests.
- **Measured locally:** 4,690 root tests passed /11 skipped,70 desktop/phone browser checks,25 native tests; strict Money and mobile types/build pass. Sixteen calendar failure cases, token-absence contract, route/client reconnect metadata and two UI-state cases reproduced first. One full run hit two unrelated Presence HTTP/timing failures;72 isolated cases and the complete rerun passed. An initial browser red run hung and was stopped; it is not claimed as red-first evidence. Calendar read failures retain previous evidence and expose retry/known Google reconnect; native source parity is included, not a distributed native update. Coverage, derived deletion, timezone/recurrence and calendar-only scheduling remain open.
- **done (#585, Codex, 2026-09-25):** Statement clarification/review and strict shape indices merged as `5a5ac318`; all CI gates passed. 4,656 root tests /11 skipped and66 desktop/phone checks. Git deployment READY (`dpl_22koQ2v9e6BuNRH8614VA59u36WE`), health200. Live phone flow verified with four browser-intercepted POSTs: answers/preview/retry/collapse/file retention pass. No live import or financial repair.

### Statement interpretation follow-through — 2026-09-25

- **done (#585, Codex, 2026-09-25):** #582 review found unsigned model direction can bypass confirmation, the web importer has no `needs` response flow, mixed missing-year dates skip rows, and model sample width is unbounded. Reproduce first, require explicit answers, preserve file/account while clarifying, cap shape input, and test the browser journey without a live financial import. Work on `codex/money-statement-clarification`; keep Claude's importer capability and known-bank path. Decision: replace three assertions that trusted model-selected `all_out`; the same unsigned 25.00 row currently becomes income or spending without a question. Model intent is not user confirmation; preserve parser arithmetic while asking explicitly.
- **Measured locally:** 4,656 tests passed / 11 skipped with disposable PostgreSQL; 66 desktop/phone browser checks; strict Money types and build pass. Independent review found a collapse/reopen draft bug, reproduced and repaired; follow-up review passed. One earlier suite run had an unrelated webhook-test socket hang-up; isolated seven-case recheck and complete suite rerun passed. Unfamiliar sheets now require a separate preview confirmation, including model-mapped debit/credit columns. Draft/file survive collapse and errors; changing file/account resets interpretation. No live statement imported. OpenRouter account settings were read without modification: paid training blocked, zero-retention routing not enabled; linkage to the production key remains unverified. `sensitiveContent` changes model routing, not provider retention policy. Fourteen additional red-first cases reject null/boolean/string/array shape indices instead of coercing them into real columns.
- **done (#583, Codex, 2026-09-25):** Git deployment READY as `cb3d1b67`, preserving #582. All CI gates passed. Integrated tests: 4,619 passed / 11 skipped. Live Today retains 37.13 EUR with corrected weekday copy; Ask holds sends while 30 real saved turns load, preserves draft and restores controls; focus has only the input underline. Android source repair included, native distribution still separate. F03 classifier remains preparatory; visual studies remain local.

### Current bounded follow-through — 2026-09-25, 10:31 UTC

- **Release candidate measured:** 4,570 root tests passed/11 skipped including disposable PostgreSQL;19 native handler tests;62 desktop/phone browser tests; app/Money/mobile type checks and web build pass. Focused lint has no errors (two existing hook warnings).
- **Android measured:** actual audit app on the current local bundle loads Month values matching web, saved Ask turns, keyboard/dismissal, masked receipt disclosure and background/resume. Real sign-in component in an isolated native harness keeps email/action above the keyboard and accepts validation taps; no external sign-in message sent. No native binary/OTA release claimed. iOS current app still renders matching figures; control-tool clicks fail, so updated iOS interaction remains unverified.
- **F03 limit:**18 classification fixtures and2,000 deterministic legacy-equivalence comparisons pass; runtime still uses existing nearest weak match. Classifier is preparation, not a resolved financial finding. No ingestion migration or data repair.


- **doing (Codex):** web Ask initial-history race. Live reload briefly exposes new-conversation suggestions/composer before the saved turns arrive; prevent sends/attachments until history settles, retain drafts, distinguish loading from empty/error. Nine behavior tests cover deferred/failed reads, the 15-second deadline, ignored late responses and cleanup. Independent review found no substantive issues. Release work isolated in `codex/money-followthrough-release`; later design studies stay outside it.

- **doing (Codex):** Android sign-in keyboard reachability and truthful native privacy footer. Live audit build on Metro8088 hides email/action under the keyboard; footer retained the old unsupported card-storage claim. Authenticated Month, Ask, Ledger and background/resume now pass in the Android audit app.

- **doing (Codex):** correct the weekday-share explanation. Live Today calls Friday usually quieter while Month shows the largest eight-week recorded total. The allowance ratio compares a weekday median against only the remaining days, not total weekday spending. Update copy/tests to name the even-split comparison; calculations and existing financial assertions stay unchanged. This is the reason for replacing the old wording assertions.

- **done (#580, Codex):** explicit WhatsApp send failures deployed; all CI gates passed. Provider delivery/recovery is still incomplete; no live messages were sent.
- **doing (Codex):** F03 ambiguity safety net: introduce and test an explicit pure candidate classification, preserving existing ingestion behavior until deferred storage, resolution and uncertainty propagation can ship together. This milestone does not fix ambiguous production attachments.
- **doing (Codex):** Ask composition remains local; round25 fresh screenshot-only scores 7.3/10 desktop and 7.1/10 phone; independent 9/10 gate unmet. Android read-only interaction passed as detailed above; updated iOS interaction remains unverified.
- **Live measured:** signed-in receipt failure/retry exercised against the deployed UI with a browser-only simulated503, followed by real200/two evidence entries; payment remains expanded, no false empty state. Plan/You/Account/Ask navigation has no page errors or Money API failures in the bounded pass. Composer focus has only the2px underline and no enclosing form outline.


### Release follow-through — 2026-09-25, 09:20 UTC

- **done (#575, #578, Codex):** Git-triggered production releases READY. #578 merges as `4fceae4a2207bc01621b1d2d360cac023baec6cc`, preserving Claude's #576 refresh-cookie repair and #577 timing evidence. No duplicate CLI deployment.
- **Measured:** integrated root suite 4,529 passed/11 skipped; 19 native handler/lifecycle tests; 60 desktop/phone browser tests; app/Money/native types and web build pass. All CI jobs and secret scan pass. The skipped CodeRabbit review is not counted as independent review.
- **Live check:** authenticated Month and health load; payment evidence returns masked card references. Browser transport disconnected before live failure/retry testing; that behavior has automated coverage. Updated local iOS bundle loads against production; full updated-device foreground/keyboard coverage and interactive Android pass remain open. Native binary distribution is not part of these web deploys.
- **doing (Codex, 2026-09-25):** screenshot-only Ask refinement remains local and outside #578. Fresh desktop scores through round15 peak at8.1/10; the9/10 gate is unmet. Direct labels and proportional bars replace the experimental misleading segmented legend in the next study.
- **doing (Codex, 2026-09-25):** bounded WhatsApp send-result handling implemented locally: ten new regressions failed first, then 91 channel/provider/pipeline tests and three quiet-failure checks passed. A resolved provider failure stops later reply parts and returns `money_send_failed`; local suppression stays distinct. Independent bounded review found no actionable issues;56 handler/pipeline tests passed in review. Release CI pending. Financial approval consumption is retained. Never release/re-execute a financial action because its reply could not be sent. This is truthful outcome reporting, not durable inbox/outbox recovery; broader W1–W3 work remains open.

**Decision:** Libraries.dev's seven public libraries and19 variants were evaluated. Retain the existing orb for real work, fix reduced-motion lifecycle, and avoid adding ornamental dependencies to payment evidence. No new package was installed. Private account screenshots and audit artifacts are not committed to this public repository.

### Audit follow-through — 2026-09-25

Stefano authorized implementation, research, design iterations and native-device QA after the September 24–25 audit. Codex works on `codex/money-beta-hardening`, based on `a10e605a`, in an isolated checkout; the owner's working tree is untouched.

| Work | Status |
|---|---|
| T01/T03: regressions, streamed greeting completion, mandatory financial reads | doing (Codex, 2026-09-25) |
| T08: reconcile production anonymous Money privileges | done (live migration `20260924230642`, 2026-09-25) — anonymous grants 119 → 0, service-role grants 189 unchanged, 27 Money tables retain RLS; ledger row count unchanged; permission catalog saved |
| T04: monetary claim guard and shared allowance evidence | doing (Codex, 2026-09-25) — integer-currency bypass reproduced; semantic grounding remains a separate larger slice |
| T03/T16 native session and transcript synchronization | doing (Codex, 2026-09-25) — three failing regressions now pass: session clear, delayed old-session write, refresh during a reply; iOS displays the web turn; keyboard clearance and supporting-payment expansion verified; pending release |
| T07: preserve issued uncertainty through scoring timeouts | doing (Codex, 2026-09-25) |
| T10: durable channel boundary and unambiguous approvals | doing (Codex, 2026-09-25) |
| T04–T12: grounding, matching, purpose, coverage, uncertainty, privileges, deletion, durable channels, refresh, series | todo |
| T16: current-register Ask evidence disclosure and native parity | doing (Codex, 2026-09-25) — current live web reopened and deployment matched; no global register replacement |
| T13–T19: scoped chat, exports, prediction evaluation, approved design, pages, dependencies, documentation | todo |
| Canvas/Blackboard data path, Instinct/Muse research, iOS/Android and page QA | doing (Codex, 2026-09-25) — iOS 26.5 Ask/Plan inspected; Android 35 audit build compiled and launched; full native QA blocked by locked Mac, not certified |

Decisions under the owner's explicit delegation: source removal must remove derived financial copies while preserving user-authored messages with unsupported answers retired; credit limits are not spendable cash; initial friend pilots use complete statements with visible coverage; calendars provide schedule context, never payment evidence or causal claims; WhatsApp starts with reliable consultation and individually confirmed edits, without unsolicited broadcasts or third-party purchases. Channel safety decision: a reaction cannot identify one of multiple offers, and consumed siblings must not change that meaning or renumber numbered replies. Expire all approval routes after the existing ten-minute window and fail closed if an inbound receipt cannot be stored; this does not claim durable completion is solved. The current live warm/spacious web register is the baseline; native screenshots are a separate surface. Ask screenshot-only critiques now reach 7.5 desktop and 7.3 phone after nine iterations; the independent 9/10 gate remains open. The purchase comparison is computed, currency-scoped, and preserved in streamed/history responses; generic follow-ups now reflect the comparison. Local verification: 4,488 full-suite tests pass, 1,314 Money tests pass (73.70% lines / 60.27% branches), 45 PostgreSQL persistence tests and 58 desktop/phone browser tests pass; web/mobile type checks and web build pass. Claude’s beta-signup #574 is merged into the audit branch without conflict; the nullable phone column exists in production, and the browser test verifies the submitted phone/source choices without sending an invitation. Application changes await release; only T08 from this audit is live.

| # | Task | Status |
|---|---|---|
| QW1 | `bankState.js`: refuse to sign or read without `JWT_SECRET`; `timingSafeEqual` | done (claude/quick-wins `ba89498a`, 2026-09-19) — 8 tests; the route answers 502 instead of signing with 'dev' |
| QW2 | Revoke `anon` on `money_*` tables (migration + catalog test) | done (`ba89498a`) — catalog test fails without the migration; found and fixed the test bootstrap loading only 17th/18th migrations |
| QW3 | `SENTRY_DSN` on Vercel + alert on `cron_executions` failures | done (2026-09-20): Stefano created the `twin-me` project in his Sentry organisation (EU region) and read the DSN; `SENTRY_DSN` (secret) and `VITE_SENTRY_DSN` (config; a DSN is public by design) are on Vercel production and apply from this deploy on; #456 reports every non-success cron run to Sentry tagged `cron:<job>`. An alert rule on that tag is one click in Sentry (Alerts, New alert, issue matching tag `cron`) |
| QW4 | Rewrite `CLAUDE.md` architecture to what exists; `AGENTS.md` = `CLAUDE.md`; CI check | done (`8df36edf`) — money first; the five gone services named as gone; `npm run sync:agents` + `agents-in-sync.goal.test.js` |
| QW5 | Delete `test/`, `ml/`, `context/`, `screenshots/`; untrack `mobile/node_modules` | done (`ba89498a`) — `mobile/node_modules` was never tracked; ignore rule added |
| QW6 | Delete or fix the 79 skipped tests | done (`812f5770`) — 9 spec files + 11 cases of deleted code removed; what remains is opt-in gates (`TWINME_RUN_*`, `RUN_HEAVY_AUDITS`, `STRIPE_E2E`) and runtime guards; suite 5,506 passing, 4 skipped |
| OW1 | **Money first**: `/`, `/home`, `/dashboard`, post-sign-in and post-OAuth all land on `/money`; the twin is a secondary link | done (`783bc68d`) — `/home`, `/dashboard`, the onboarding gate; pinned by `money-first.goal.test.js` |
| OW2 | **Orbs exactly as the library**: monochrome ink per the library's own theme rule; chat avatar orb at 64 while thinking; every loading state on the money surface is an orb | done (claude/orbs-exact, 2026-09-19) — painter is the library's `M = round((dark ? 1-white : white)*255)`; measured on the built page: darkest dot rgb(26,26,26), 0 non-neutral pixels; chat pending orb at 64 |
| OW3 | **Codex protocol** live: this file, `sync:agents`, branch prefixes, the Decisions rule | done (`8df36edf`) — protocol in `CLAUDE.md`, copied to `AGENTS.md` |
| OW4 | **Scouting**: three background scouts (products, papers, social) reporting candidates into `docs/intel/` through the `/intel` rubric | done (2026-09-19) — 33 candidates triaged: 3 DISCUTIR (INTEL.md, Em aberto), 1 PROTOTIPAR (`dia-hurdle`) + 2 fusions (BACKLOG.md), 10 Radar, 17 descartes in `seen.jsonl`; the seven synthesised ideas are listed below for Stefano |

## The chat, curated (since 2026-09-20)

Stefano's ask of 2026-09-20: specific questions, many scenarios, judge the answers, fix until
they create value. The harness is `scripts/money/chat-eval.mjs` over
`tests/api/services/money/chatScenarios.js` (the real model against the real ledger, seven
checks and a judge). Each round: what was asked, what was wrong, what changed, the tally.

| Round | Asked | Was wrong | Changed | Tally |
|---|---|---|---|---|
| 1 (2026-09-20) | last night, yesterday in total, this week, this vs last week, each day, biggest yesterday | lines one by one and no total (the rules forbid adding, the context held only the month) | `windows.js`: today, yesterday, last night, this week, last week, last 7 days and each day, totalled in Madrid days and quoted; a week figure (a bar per day); greetings answered without the model (a greeting took 30 s and a 504) | 32/35 with the judge, then 5/5 on the smalltalk rerun; "Ontem você gastou 58,00 € no total, em 4 pagamentos" |
| 2 (2026-09-20) | food yesterday, coffee this week, where most this week, a graph per day, transport last week, can I afford 60 tonight | a stretch with a kind of place or a place in it had no line to quote; "a graph of this week" drew the weekday shape | each window now says its kinds of place and its places (`breakdown()`); the rule names the week figure for "each day" | 4/6 by the harness; the two judge-0 answers are right by Claude's reading: "nothing on food yesterday, the 58,00 € was entertainment", "coffee is not a separate kind this week; eating out 14,13 € at Glovo" |
| 3 (2026-09-20) | an offer tapped: a trip to remember, then a follow-up, then forget; a payment that is not mine, then the ledger, then revert | remember works end to end (fact stored, on the page, the follow-up knows it, forgotten again); "the X payment of N is not mine, my flatmate used my card" timed out at the model's 30 s and the chat then said the ledger could not answer (false); when it did answer it offered only a note | the model may take 50 s under the route's 58; a timeout says "That took too long to answer. Ask it again"; a not-mine statement always carries the not_me offer for the payment named, computed from the words | not-mine-flatmate, flatmate-spotify, savings-transfer 3/3; the trip is kept as a note without dates (calendar_ahead stays 0): next round |
| 4 (2026-09-20) | "I am going to Bilbao next Friday to Sunday with two friends", remembered, then "what will next weekend cost me?" | the trip was a note with no days: nothing on the calendar, nothing for the covariates, the follow-up could only repeat the words | `when.js` reads the days a sentence names (tomorrow, this/next weekend, Friday to Sunday, the 25th, 3 to 6 October, in en/es/pt) and a remembered trip also writes a note on each day titled "Trip: ...", which the calendar's away rule reads; the reply says "those 3 days are marked as away" | day notes written for 25 to 27 September and forgotten again; the follow-up says honestly that the ledger has no learned cost for a trip yet; Today's number does not move (no learned cost), and the week ahead (You page, the chat's calendar lines) lists the trip days beside Google's own events, and without a calendar too |
| 5 (2026-09-20) | how much is left this month, when is rent due, Glovo this month (times and total), am I on track, groceries vs August, "why?", English on a Portuguese account | no plain "left" figure; no per-place monthly total, so the model composed "Glovo 3 times" from other lines' numbers (each amount grounded, the statement false); "biggest payment" answered with a place's two-purchase total | a "Left for the N days until X" line from the same allowance the day rests on; each place's month with its count, forty places; a rule that a missing place had no payment and that the biggest payment is one line | "sobram 147,73 €"; "Você pediu Glovo uma vez este mês, por 14,13 €"; groceries vs August right; rent honestly unknown (no rent fact) |
| full run (2026-09-20, after round 5) | all 49 scenarios with the judge | 41/49: five are the judge rating honest answers 0 (nothing on food yesterday; coffee not a separate kind; no rent line; two follow-ups that quote the facts given), two the week figure drawn as the weekday shape, one an over-long keep answer | the per-day ask now draws the week figure in code (`asksPerDay`), whatever the model named | checks: figures 47, actions 49, length 48, style 49, grounded 49, mention 49 of 49 |
| 6 (2026-09-20) | on Saturday, on the 15th, between the 8th and the 14th, since the 1st, this weekend versus last, last Friday night, per week on average, Glovo count, most expensive, Bizums by person, Mercadona this week | only six fixed stretches had a line: a range got one of its days, "Friday night" was Friday's whole day, a weekend comparison answered with one payment; "per week on average" quoted the last seven days; the largest payment was "at unknown"; the model called a Lidl a Mercadona; "each day" arrived with an emoji | `asked.js` reads the stretch a question names in en/es/pt (a weekday, a night 18:00 to 06:00, this or last weekend to Monday 06:00, a date, a range, since a date) and `stretchLine()` totals it with its kinds and places; `weekAverageLine()` (last four full weeks, each named); an unnamed payment is "a payment without a name"; `plainWords()` strips every emoji on the way to the wire (object, prose and stream); rules: quote the asked stretch, never say one place is another, answer "can I afford X" yes or no first | 8 new scenarios (57 total); 15/19 on the affected set with the judge, then 3/4 on the re-run; the four judge-0 answers are right by reading ("nothing on food yesterday", "no, 13,43 EUR safe today against 60", "nothing at Mercadona") |
| 7 (2026-09-20) | Bizums this month and to whom, who sent me money, a graph of the 8th to the 14th, last weekend day by day | the month to and from people had no total (five people listed, "no single total"); a graph of a named stretch drew the last seven days; the model offered to remember a thirteen-word question and closed with "want a bar chart?" under the bars it had asked for; the harness counted "Maria D." as a sentence | `monthBetweenPeople()` + two lines (to people / from people, by person, this month); `dayTotals()` + `askedDays()`: the week figure draws the asked stretch, a bar per day, clipped to its hours; a long ask that opens with a question word is not a statement; a remember rides only on a statement; `withoutChartQuestion()` drops a sentence that asks about, or tells them to ask for, a figure already drawn; the sentence counter skips an initial | 4 new scenarios (61 total); 8/8 on the affected set after the fixes (bizum-month, who-sent-me, graph-range, weekend-by-day, by-day, graph-week, between-dates, weekend-vs-weekend) |
| 8 (2026-09-21) | most expensive day this month, which day of the week, August in total, payments yesterday, Corte Ingles this month, biggest subscription, when the money runs out, left at month end, "and on food?", "and the weekend before?", transport today, food this week vs last | "which day of the week" was answered with one week's Monday (the only per-day line was the last seven); the month's costliest day had no line; "biggest subscription" got the whole recurring list; "the weekend before" re-dated Saturday's line as 5 and 6 September (grounded amounts, wrong days) | `costliestDayLine()` and `weekdayLine()` (last eight full weeks, costliest first); the recurring short circuit names the largest for biggest/largest/maior/mayor, in three languages, and hears "assinatura"; `asked.js` reads "the weekend before", "two weekends ago", "el fin de semana anterior", "o fim de semana anterior"; a rule: a line is about the days it names, never another's | 4 new scenarios (65 total); 6/8 on the affected set: by-day asked at 02:00 on a Monday found nothing this week (honest), and the style check called the merchant OpenAI "AI talk" (harness fixed) |
| 9 (2026-09-21) | last thing paid, received this month, did my parents send the money, average per day, OpenAI since July, Hayque times, the 3rd of August, July in total, cheapest day, what to cut to save 100, left on the 30th, Spanish week, Portuguese last night, Sunday morning | "did my parents send" was yes on a 90-day family total whose last transfer was in August, then yes on a friend's transfer; average per day had no line; "since July" at a place got this month's figure; the cheapest day came from the last seven; a morning had no stretch; a Spanish question was answered half in Portuguese and an English one in Spanish | `describeBetweenPeople` says "last on"; the month-to-people lines carry roles and say "Nobody marked family sent anything this month"; `monthPaceLine()`, `cheapestDayLine()`; a whole-ledger by-place line with its first day; `asked.js` reads a morning (06:00 to 12:00) and an afternoon (12:00 to 18:00); `languageOf()` reads the question's language from its small words and a one-line hint sits right under the context; an empty stretch says "not one payment" | 6 new scenarios (71 total); 8/10 on the affected set with the judge, both misses the judge's (a correct "no, none marked family"; a correct "nothing Sunday morning" that then passed twice) |
| 10 (2026-09-21) | did I pay rent, what is still committed, which subscriptions to cancel, why is today low, what changed since yesterday, Revolut this month, was last week more than usual, "no, I meant last month", can I return anything, Glovo and Uber Eats together, transport this week and last (pt), what is left (es) | the model paired a kind's total with the window's smallest payment as "the largest"; the window's by-kind list was capped at four kinds, so transport fell off last week's line and the chat said there was none; the people line listed nine names; "vou pedir o grafico" and "I'll draw the week" narrated a figure already drawn | each kind carries its own largest payment in the window lines; every kind is listed (places stay at three); the people line names six and counts the rest; narration of a figure is dropped like a question about it; a rule for a pair of places when one is absent | 5 new scenarios (76 total); 8/8 on the second run; the correction ("no, I meant last month") answers August and the judge, which does not see the history, rates it 0 |
| 11 (2026-09-21) | the 22,36 payment on Friday, what my flatmate owes me, split Saturday's drinks three ways, a Zara refund, Higgsfield next month, when my parents send money, days until I run out, nonsense, the airport, Mercadona (es), bares (pt), then "and in August?", then "which was the biggest?", "show me the maths" | an English sentence with "a refund" tied English with Portuguese on the word "a" and was answered in Portuguese; "bares" is not a kind's name, so the chat said nothing was spent in bars while eating out held 112,46; three turns in, "which was the biggest?" named the month's largest payment (a supermarket) as the largest bar bill | no one-letter or English-looking words in the Portuguese list, more English words; a computed "Largest payment per kind" line for this month and last; one line of the words people use for each kind (bar, cafe, supermarket, metro...) | 3 new scenarios (83 total); 11/12 with pass^2, the miss an honest "nothing seen at Zara" the judge rated 0; the split offer, the maths behind the number, the nonsense and the airport were right first time |
| 12 (2026-09-21, Stefano's own thread) | "quanto sao gastos em software todo mes", "me crie uma tabela com os gastos de software com o mais caro pra baixo" | both answered "O livro nao consegue responder isso com o que tem" in production; the logs show the streamed model run ending at 22 s with 0 tokens: the reasoning patience fired, and the gateway ends an aborted stream quietly with empty content instead of throwing, so the fallback without reasoning never ran and the empty answer became the fallback line. The plain endpoint (what the harness tested) never took that path. Also: a Portuguese imperative ("me crie") and a mid-sentence "quero saber" read as statements and earned a remember offer; "todo mes" fell into the recurring shortcut; the model said it cannot make a table | the empty-after-patience case retries without reasoning (a test mocks the gateway's quiet end); `chat-eval.mjs --stream` runs scenarios through the app's own streaming path; openers and an anywhere-ask for pt/es imperatives; the recurring shortcut yields to a named kind; a table of one kind is the shares figure by place within that kind (`asksTable`, `kindInMessage`, `category` on shares) | 2 scenarios from the thread (85 total); on the streamed run of 16 answers the new fallback fired 4 times and every one answered; open: one streamed answer carried an average the harness could not ground (196,94), to be traced |

Not there yet, honestly: no web access (the chat says only what the ledger computed); figures are bars, shares, the band, months and recurring, not free-form diagrams; an offer (remember, not_me, person, answer) changes the ledger only when tapped, which the browser suite covers.

## Milestones (from the 19 September audit — `.claude/plans/2026-09-19-repo-audit/README.md`, published at https://claude.ai/code/artifact/ae6cd0ba-b090-4660-85ff-cfadb9323679)

### Milestone 0 — safety net

| ID | Task | Acceptance | Status |
|---|---|---|---|
| M0-1 | Coverage as a number for `api/services/money` and `src/pages/money`, threshold at today's value | CI fails below baseline | done (claude/loop-and-coverage) — `vitest.money.config.ts`: lines 65.19%, functions 58.78%, branches 52.41%, statements 61.52% over 67 files / 815 tests; floor 65/58/52/61 in CI |
| M0-2 | Find the order-dependent flake behind `--retry=2`; remove the retry | ten green nightlies at `--retry=0` | doing (Claude): 1 of 10 as of 2026-09-22. The full suite passed locally at `--retry=0` (457 files, 5 565 tests); the nightly runs it without retries as a graded job ('Unit suite, no retries'). Only one *scheduled* nightly has run it so far (2026-09-21, green); the rest of the run list is manual dispatches, which is why a pass rate could not answer this. The report card now prints the consecutive streak ('N in a row'), so the gate reads itself: drop `--retry=2` in ci.yml when it says 10 in a row |
| M0-3 | Import-time canary: every `api/services/money/*.js` imports in < 2 s | fails today on `store.js` | done (claude/loadable-core, #432) — `tests/goals/money-imports.goal.test.js`, exception list empty |
| M0-4 | Backup rehearsal before ledger-touching migrations | restore proven once | done (#438, 2026-09-20): `scripts/money/backup-rehearsal.sh` (`dump`, `restore`, `verify`, `rehearse`): pg_dump of the public schema, restore into a fresh database with the roles and `auth.uid()` prepared, then every `money_*` count and an md5 of every ledger row compared. Proven locally (9 rows, identical) and on every CI run by the persistence test. For production: dump from the Supabase direct connection, restore into a local Postgres 16, verify; the runbook is the script's header |
| M0-C | Coverage floor for everything the product reaches | CI fails below the floor; the set is the reach set plus `src/` | done (2026-09-22): `vitest.staying.config.ts` measures every API file reachable from `staying-roots.txt` and all of `src/` over the whole suite (505 files): lines 36.24%, statements 36.71%, functions 36.68%, branches 32.44%; the floor is set a hair under and can only rise. The money core keeps its own, stricter floor (M0-1). |
| M0-B | A rendered page walked before merge | a pull request cannot merge if Today, Month, Account or Ask fails to render | done by the existing `Money browser` CI job (recorded 2026-09-22): it builds the site and walks `/money`, `/money/month`, `/money/account` and `/money/chat` on every pull request (`tests/e2e/money-beta.spec.ts`: no browser errors, no horizontal overflow, the outage and empty states distinct). Previews were never needed for it, which is what let D21 turn them off. The production canary walks the live site nightly. |

### Milestone 1 — critical fixes

| ID | Task | Acceptance | Status |
|---|---|---|---|
| M1-1 | QW1 + QW2 + QW3 | as above | see Now |
| M1-B | Validate every mounted write (the staying set after D20) | 400 with a named field on a bad body | done (2026-09-22): 32 write routes across 14 files wired to `validate()` with `stayingSchemas.js`, types and lengths only; the goal test names every staying file and each exemption with its reason |
| M1-C | `script-src` without `'unsafe-inline'` | zero CSP script violations on sign-in and the money pages in production | done (2026-09-22): the inline theme script left `index.html` (the register is light everywhere; ThemeContext stamps after mount), the sixteen Claura prototypes under `public/cinematic` and two dev pages went with their inline scripts, `script-src` is `'self'` and PostHog. The walk on the built site with the production header found no script violation and one real bug: `SunContext` called `api.open-meteo.com` (blocked by `connect-src` since the header shipped) and posted GPS to `/api/location/current`; it never painted anything since 2026-09-01, so it and the day/night background are deleted. `tests/goals/script-csp.goal.test.js` pins all three. |
| M1-D | Name every swallowed error in the staying set | grep count 0 across everything the money routes reach | done (2026-09-22): 73 anonymous `.catch(() => ...)` and 2 empty catch blocks across 36 files now go through `quietly('name', fallback)`, which moved up to `api/services/quietly.js` (the money path re-exports it). `tests/goals/quiet-failures.goal.test.js` walks the reach set (no comments), fails on any anonymous catch or empty block, and on two sites sharing a name. |
| M1-2 | `validate(schema)` middleware; every money route, then top-20 legacy | 400 with a named field on a bad body | money done (2026-09-19): `api/middleware/validate.js` (zod; body, params, query; 400 `{ error: 'Invalid request', field, message }`), `api/routes/moneySchemas.js`, 18 write routes wired, 15 bad-body cases tested, goal test holds every write route validated. Legacy slice done (2026-09-20): sign-in (6 routes, `authSchemas.js`: types and lengths only, the handlers keep their own messages), the extension (4) and the directives (2); the goal test covers the four files. Presence and twins-brain left alone: Codex's active area and a parking candidate (D1) |
| M1-3 | Name every swallowed error in money (`.catch(() => null)` → counted outcome) | grep count 0 in money | done (#434, 2026-09-19): `quietly(name, fallback)` in `api/services/money/quietly.js` logs and counts each; the 26 anonymous catches are named (readings/facts, bank-callback/record-refused, ...); `tests/goals/money-quiet-failures.goal.test.js` holds the count at 0 and the names unique |
| M1-A | Unmount the parked twin (410 behind the D1 switch, app pages parked) | write handlers reachable fall to the staying set | done (D20, 2026-09-22): 114 API prefixes and 50 page patterns parked; goal test holds the lists complete |
| M1-4 | Revocation on serverless: fail closed when Redis is down (recommended) | documented + tested | decided **no** (D19, 2026-09-22): fail open, never silently; the two empty catches in `auth.js` now log the fallback. Related, done 2026-09-21: every Supabase request now has a 25 s deadline (`api/config/timedFetch.js`, `SUPABASE_REQUEST_TIMEOUT_MS`), after a refresh hung sixteen minutes on a dropped link |

### Milestone 2 — high-leverage

| ID | Task | Acceptance | Status |
|---|---|---|---|
| M2-1 | Break the money cycles (`store.js ↔ calendar.js`, `store.js ↔ predictions.js`) | `madge --circular` = 0; M0-3 passes | done (#432) — `factsRepository.js` + `forecastService.js`; madge 0; bare import of `store.js` 252 ms where it never returned; store.js 1,237 → 1,108 lines |
| M2-2 | Split `MoneyForAccount` into a hook + three views | longest function < 250 lines | done (#434, 2026-09-19): `useMoneyAccount.ts` (three hooks + one composing), `views/{Today,Month,You}View.tsx` and their sections; same innerText on every view before/after in a Playwright walk at two viewports; `tests/goals/money-page-shape.goal.test.js` holds the 250 line limit |
| M2-2b | The three functions still over 250 lines: `MoneyConversation` (325), `MoneySetupPage` (409), `readingWords` (375) | each < 250; the goal test ratchets them | done (#434, 2026-09-19): `readingSayers.ts` (one sayer per kind) + `readingHelpers.ts`; `setup/useSetupQueue.ts` + `setup/AnswerFields.tsx` + `setup/setupWords.ts` (page 94 lines); `chat/useConversation.ts` + `chat/useLedgerTrace.ts` + `chat/askLine.ts`. Ratchet list empty; Ask asked and answered and Setup showed its first question in a Chrome smoke on the production build |
| M2-3 | One read per view, cached per ingestion revision | Today cold < 1.5 s | done (#434, 2026-09-19): `GET /api/money/page` reads the eleven parts once under one authentication (`pageRead.js`; the day reuses the month's forecast), the hook reads it once and keeps the last read per tab for an instant paint on reload, the auth middleware remembers the Redis revocation answer for 30 s (350 ms → 1 ms per request here), and index.html serves Geist itself instead of six blocking Google stylesheets (first request at 65 ms instead of 4–10 s). Local production build, warm API: Today painted in 857 ms and 1 368 ms (was 13–19 s), 1 page request (was 9 reads + preflights). Cold-server spikes here are the local Redis/Supabase link; production after #434 (2026-09-19, signed out, Chrome from Madrid): the document answers in 0.1–0.9 s, `/fonts/geist.css` in 0.22 s from the same origin, the 190 KB entry in 1.3 s; the first API request leaves at 3.9 s after the page's own load and PostHog arrives after it, blocking nothing. Signed in on production (the nightly's canary, first real run 2026-09-20 12:xx UTC): Today painted in 10,9 s on a cold function and 3,2 s warm, with 2 money requests (`/money/page` and the stale check). The canary now records both and holds the budget on the warm one |
| M2-A | One read of the ledger and one of the facts per page, on the server | `money_ledger_page` walked once and `money_facts` read once per `readPage`; the same for the chat's gather | done (2026-09-22): `readPage` and `chat.gather` read both once and hand the rows to every part through `given` (`forecast`, `months`, `todayAllowance`, `refreshRecurring`, `categorySpend`, `subscriptionUsage`, `accountsWithCards`, `inboxAddress`); `selectTransactions` makes the same slice the RPC did. Measured on the real ledger: 7 ledger walks and 7 facts reads per Today became 1 and 1, 37 Supabase calls became 25; warm wall time unchanged locally (about 350 ms, the parts run in parallel). `tests/goals/money-one-read.goal.test.js` counts the calls on the real page. Client half: `ProtectedRoute` lets `/money*` mount before the verify round trip for a person the browser already knows, and every money read waits for the token (`accessTokenReady`) instead of leaving first and painting a 401; the production canary's warm and cold numbers after deploy are the measurement. |
| M2-B (front) | Delete every file under `src/` the entry does not reach | `scripts/ci/front-reach.mjs` lists nothing; a goal test keeps it there | done (2026-09-22): the retired twin's 52 routes left `App.tsx` (the gate still answers their addresses with the parked line), and 308 of 540 files under `src/` and 15 of their tests went with them. The walker follows bindings, side-effect imports, re-exports, dynamic imports, the `@/` alias and CSS `@import` (the API walker's regex ran across statements and missed side-effect imports; bounded). Type errors 109 -> 31, lint errors 66 -> 46, built chunks 97 -> 60, built JS 4,452 KB -> 3,192 KB. The 31 type errors left are in live files (`enrichmentService`, `OAuthCallback`, `useExtensionSync`, `AnalyticsContext`) and are M2-C's. |
| M2-B (api) | Delete the parked API behind the 410 gate | done (this PR, 2026-09-24, pulled forward from 22 October on Stefano's directive of the same day): 331 files under `api/_app/` that no staying root reaches, 140 imports and 158 mounts in `server.js`, the parked twin's startup jobs (platform polling, Bull queues, token lifecycle, pattern learning, token-expiry notifier), and 117 tests that imported them. What `server.js` itself needs on every request (request timeouts, sanitization, the error handler, the circuit breaker, the OAuth rate limiter, the websocket server, the gate) is in `staying-roots.txt` now. `LEGACY_TWIN_ROUTES` stays as the map of 114 addresses the route gate answers with 410; the goal test asserts none is mounted again. `.env.example` lost 23 keys; `routesWithDirectFrom` 93 -> 1. |
| M2-C | Strict types and zero lint errors for what stays | `tsc -p tsconfig.app.json` with `strict`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch` reports 0; `eslint .` reports 0 errors; the baselines read 0 and 0 | done (2026-09-22): the 31 type errors left after the deletion were one wrong `User` field in analytics, a `chrome` global with no declaration (now `src/types/chrome-extension.d.ts`), a headers type `fetch()` would not take, a `Uint8Array` buffer, a window guard and a voice context type; turning strict on added 31 more (the lazy loader's generic swallowed component props, an untyped OAuth state, 18 unused locals) and each is fixed, not silenced. Lint: the 45 errors were `any` in the unreferenced `api/mcp-server` (deleted) and in 16 stale root-level Playwright specs (deleted, none run by a workflow), ten `case` bodies without braces, six useless escapes, two emoji classes with combining marks (now `\p{Extended_Pictographic}`), and five intentional control-character strips that now say so. The ratchet stays for `routesWithDirectFrom` (112, M2-D). |
| M2-D | A data-access layer for what stays | no route file the product reaches builds a table query inline; the ratchet's `routesWithDirectFrom` counts only parked files | done (2026-09-22): the 125 direct table calls in the 23 staying route files (auth-simple 36, account 19, billing 13, beta 10, calendar-oauth 8, extension-data 8, and fifteen smaller) are named functions in eleven stores: `auth/authStore` (users, refresh tokens, magic links, pending codes, connections), `account/accountStore`, `billing/billingStore`, `beta/betaStore`, `calendar/calendarStore`, `extension/extensionStore`, `messagingChannelStore`, `agentActionStore`, `consentStore`, `transactions/userTransactionStore`, `opsStore` (health probes, the cron ledger, retention). Each is a thin builder returning the same `{ data, error }` the route awaited, so control flow and error handling did not move; the routes lost their client imports (two of them held a second service-role client from `config/supabase.js`). `routesWithDirectFrom` 112 -> 93, all parked files, gone with the API half of M2-B. `tests/api/services/stores.test.js` and `auth/authStore.test.js` record every chain. |
| M2-4 | Separate the bank read from the daily loop (own cron, own budget) | `learnSkipped` = 0 for a week | done (claude/loop-and-coverage) — `/api/cron/money-learn` daily 05:30 UTC for every person with a ledger; 4 tests; cost canary green |
| M2-5 | Park the legacy twin behind `LEGACY_TWIN_ENABLED`; money CI lane | money CI < 3 min | switch built (2026-09-20), flip is Stefano's (D1): `api/middleware/legacyTwin.js` stands in front of the ten twin crons; `LEGACY_TWIN_ENABLED=false` on Vercel makes each answer 200 and do nothing, pages stay reachable; anything else changes nothing. Test holds the ten gated and the money crons not. The money CI lane already exists (Money persistence, Money browser, strict types, coverage floor) |
| M2-6 | Agentic OS phase 2: report card for every goal, money canaries, `loop.sh` triage → plan → implement → inspect | grades gate unattended runs | slice 2 done (2026-09-20): when triage says attention, the `implement` job runs Claude Code headless on the plan on `loop/<date>` (edits and tests only, $5 cap, `guard.mjs` refuses any change to the ledger's tables, the feed, the schema, the crons, the workflows or .env) and opens a PR labelled `loop`; the `inspect` job has a fresh model read the diff against the plan and leaves its verdict as a comment and a label (`loop:approved` / `loop:needs-work`). The loop never merges: the report card's eligibility informs a person, and money is never written by it. `ANTHROPIC_API_KEY` is in the Actions secrets. Rehearsed end to end on 2026-09-20: a hand-written plan went through triage to the implement job, Claude Code (Sonnet) wrote a 13-line test on `loop/2026-09-20` and opened #444; the inspect job (Opus) ran the test, checked the diff's scope and approved with one honest caveat. First run found two faults, both fixed in #443: Actions could not open PRs (repository setting) and the stage committed its own plan file. Slice 1 (2026-09-19): report card for every nightly job, graded jobs, triage on Actions |

### Milestone 3 — quality

| ID | Task | Status |
|---|---|---|
| M3-1 | `strict: true` for `src/pages/money/**` | done (#434, 2026-09-19): `tsconfig.money.json` (extends the app's, strict, the money pages + MoneyOnboarding, LedgerOrb, Wait) passes with 0 errors after 15 fixes; checked in Build & Test and in the nightly 'Money first' job |
| M3-2 | Lazy-load every non-money route; drop `vendor-elevenlabs` from the money bundle | done (#434, 2026-09-19): the start warms only the money pages (the legacy twin's TodayPage, TalkToTwin, MoneyPage and recharts were fetched at 0 ms on every screen); PostHog arrives on idle behind an ordered queue (209 KB out); each dictionary only for its language with a first-frame guarantee (119 KB out); the front door, the OAuth return, the 404 and the legacy sidebar are their own files. Entry chunk 185 KB, was 556; the money screen loads no legacy chunk and `vendor-elevenlabs` (469 KB) is reached only from the onboarding's voice interview. Measured on a production build in Chrome: `/`, a missing page, `/money` and `/` signed in all render, 0 errors |
| M3-3 | One Redis client | done (#434, 2026-09-19): the OAuth rate limiter rode a second connection from the `redis` package, and its Redis store was built after the limiters were, so no limiter ever held it (every Vercel instance counted alone). Each limiter now has a store that is the shared ioredis client when it is up and memory when not, decided on first use, with its own prefix; the `redis` package is gone |
| M3-4 | Regenerate `.env.example` from code | done (#434, 2026-09-19): `scripts/env-example.mjs` scans api/ and src/ (154 keys, 13 areas, the four required first, notes kept); was 109 undocumented and 16 dead; `tests/goals/env-example-in-sync.goal.test.js` holds it |
| M3-5 | Resolve `--legacy-peer-deps` | done (#434, 2026-09-19): the one conflict was `lovable-tagger` (a Lovable-era dev plugin wanting vite 5 against vite 7); removed, the lock regenerated strictly, `.npmrc` and every `npm ci` in CI and the nightly without the flag |
| M3-6 | Real-backend Playwright canary for the money journey | done (2026-09-20): `MONEY_CANARY_TOKEN` and `MONEY_CANARY_URL` set, the nightly's 'Money on production' job ran green on twin-ai-learn.vercel.app (cold 10.9 s, warm 3.2 s; the budget is on the warm walk). Was: built, waiting on a secret (2026-09-19): `tests/e2e/money-production.spec.ts` + `playwright.production.config.ts` walk Today (figure within 6 s, at most 8 money requests, one page read), Month and You on twin-ai-learn.vercel.app as a signed-in person; the nightly job 'Money on production' runs it when `MONEY_CANARY_TOKEN` is set and says so when it is not — open question 7 |
| M3-7 | Where a person is comes from their data, not the deployment | stage one done (#536, 2026-09-23): `profile.js`, country and language threaded into the lookups, the judge and the bank routes. Stage two done (2026-09-23, this PR): the timezone. Every `zone.js` helper takes a zone and otherwise reads the request's (`withZone`, an AsyncLocalStorage); the money router sets it per request from the cached profile, the two crons per person, the WhatsApp inbound per message (`inPersonZone`), so the 96 call sites in 26 files changed nothing and all moved. The page reads its own days in the zone the payload carries (`profile` on the page read): today's day for the pace bars, the day and hour of every mark on the orbits, the day a late income last came, which were UTC (a day behind in Madrid between midnight and two). Stage three done (2026-09-23, this PR): the currency. `scope.js` holds the person for a request (the zone moved onto it); `ledgerCurrency()` reads the person's currency from their accounts, `ours()` and `money()` follow it, and the twelve `currency: 'EUR'` reads (repositories, forecast, predictions, home, chat, calendar, the plan route, the phone and email row defaults, the statement account) are the ledger's own currency; the chat asks "how much is that in dollars" for a dollar ledger and keeps the code where the euro had a glyph. The page: `euro()`, `ownCurrency()` and six filters follow `profile.currency`. Nothing converts: a row in another currency is refused, as before. Done: M3-7 closed. |
| M3-8 | The bank gate is a capability per person, not an allowlist | done (2026-09-23, this PR): `capabilitiesFor(userId)` computes the bank from facts: the aggregator configured; the person already linked (an `enablebanking` account row), or listed, or the application unrestricted and their country served (the application's own `countries`); `why` names the reason (`unconfigured`, `linked`, `listed`, `restricted`, `country`, `open`, `unread`) and Sources says it in plain words in three languages. Enable Banking's `GET /application` carries no restricted-mode field (checked against the sandbox app and the reference), so that one fact is `ENABLE_BANKING_UNRESTRICTED`, set by the owner when they confirm. Phone capture opens to everyone signed in (D23). The 403s say the reason too; a closed gate never reads as "beta" again. |
| M3-9 | The Ask benchmark: 109 questions, statements and sequences in ten dimensions, scored and graded | done (2026-09-23, this PR): `tests/api/services/money/chatBench.js` + `scripts/money/chat-bench.mjs` + `chat-bench-report.mjs`. Checks per turn: figures drawn or rightly absent, offers, length, style, every amount grounded, what is named, speed, the offer taken; a grader (gemini-2.5-flash) on addresses, figure fit, structure, honesty and, on the streamed questions, reasoning; sequences teach, take the offer, ask again and are undone. First pass 77/109 in the morning; 104/109 on the day's last full run (run 6), and 7 of the 8 affected questions rerun after the last two fixes, the miss a computed reply at 3,0 s against a 3 s budget: the ledger read alone is 1,5 s. What it found and fixed the same day: US-format amounts slipping the grounding gate; where-it-went for a named month, accents, Portuguese; a weekdays figure for "which day"; statements no longer forced a kind figure; the recurring shortcut swallowing "least worth it" (operator precedence); stated commitments in "what comes back"; keep, not mine, recategorise and forget offers computed; `setPlaceCategory` wrote two columns the overrides table lacks (every recategorise tap 500'd in production); "hi" waited on a full ledger read; the scoring reconciliation hanging every read for 27 s (2,5 s deadline); subscription use in the chat's context ("least worth it" answered by size); two months added up, a person by role, a suspected duplicate and a month ahead answered by computation; the history figure forced for "over time". Left: model latency (DeepSeek p50 5 s, p90 25 s, spikes to 47 s; the page says "That took too long" at 50 s), no per-week figure, follow-up suggestions still a static list of six. |
| M3-10 | Bank alert mails without a Gmail scope: forwarded to the ledger address, with Gmail's confirmation caught | done (#549, 2026-09-23): the inbox already parsed Santander's movement mails; what stopped a person was Gmail's forwarding confirmation, mailed to the address they cannot read. `forwardingConfirmation` (Google's sender only) keeps the code and the confirming link as a `money_notices` row (kind `forwarding`, two days); `GET /inbox` carries them; Sources shows the request under the address with one Confirm link, and the inbox line names the bank's alert mails. From Stefano's four ideas of 23 Sep (plan `2026-09-23-instinct-walkthrough`): next the statement attachment by mail, the due-charge reading, the month as a sheet. |
| M3-11 | A statement or a receipt attached to a mail sent to the ledger address is read | done (this PR, 2026-09-23): `mailAttachments.js`. Resend's attachment link is fetched; a CSV or Excel goes through the page's importer into the one account the person has (evidence check and refresh as the page does), or is held as a `money_notices` row (kind `statement_mail`) that Sources shows when there are several accounts to choose from, since a mail cannot carry that choice; a PDF or photo goes through the same reader WhatsApp and the attach route use. One failing attachment fails only itself; the body is still read as a receipt or a bank alert when no attachment read. Every side effect is a dependency, tested with fakes. Idea 2 of the four. |
| M3-14 | Disconnect means gone: a bank account can be removed, and what it read goes with it | done (this PR, 2026-09-24): `DELETE /money/bank/accounts/:id` (`removeBankAccount`): the account's sightings and transactions are deleted, then the row (accesses and leases cascade), the consent is ended at Enable Banking unless another account of the person shares the session, and the recurring charges and readings recompute. Two taps on the account row: the question names the account, then Remove or Keep; the note says how many payments went. Sources had promised "remove a source and what it read goes too" with no way to remove a bank; Instinct's one real criticism in the walkthrough was mail kept after disconnecting. |
| M3-16 | WhatsApp hears a voice note, and shows it is working | done (this PR, 2026-09-24): `hearing.js` writes a voice note down (OpenAI transcription, the model the voice bridge uses, `OPENAI_API_KEY`; the audio is read in memory and dropped) and the words are answered as the typed question would be; without the key the channel says it cannot hear voice notes yet and asks for the words typed; a note it could not make out is said so too, in three languages. The read receipt now carries the typing indicator from the first moment (Kapso `markRead` with `typingIndicator`), so the ledger's silence reads as work. Instinct's walkthrough: voice notes and the visible "working". |
| M3-17 | The chat's ledger read waits only on what it needs | done (this PR, 2026-09-24): `gather()` ran five waves of reads, each waiting on the one before, though eight of the thirteen need nothing at all. Every read now starts as early as its own rows allow and the answer waits for all of them at once. Measured warm on the owner's ledger: 1,907 ms -> 759 ms, on every turn, computed or not; the reads themselves cost 0.8 s, so the wait is now the reads. The benchmark's speed failures were computed answers at 3,3-3,7 s against a 3 s budget. |
| M3-18 | "En espanol, por favor" is answered, not refused | done (this PR, 2026-09-24): a message that only asks for a language (`languageAsk`, the three names in either accent) re-answers the question before it (`questionBefore`) in that language, computed again from the ledger. The answer being replaced is left out of the prompt: the model read its own earlier sentence, repeated a figure the ledger had moved past (188,34 against the day's 190,04) and the grounding gate dropped every sentence, so the reply was "the ledger has no total for that" in whichever language it came in. Found by the benchmark (fu-in-spanish, its one failure at 108/109) and worse than it showed: English asked for in English failed too. |
| M3-19 | The suggestions survive a reload | done (this PR, 2026-09-24): what an answer suggested asking next is kept with the turn, in the envelope the receipts have always ridden in (the `figures` column), so no migration; `listChatTurns` hands it back and the page restores it with the line. Before this a reloaded conversation fell back to the fixed six, the gap left open when the suggestions were computed (M3-15). Round trip checked against the running API: asked, answered, kept, read back. |
| M3-13 | A reaction on WhatsApp is an answer: a thumbs-up on the message that carried the offers takes the first offer | done (this PR, 2026-09-24): Instinct reads a thumbs-up on its question as the yes; here the webhook parses a `reaction`, the pipeline lets it through for money people, the offers remember the message they rode in (`message_id` on `money_channel_offers`, noted when the send answers with its id), and a thumbs-up, check or ok takes the first untaken offer of that message as a tap does, through `act()`; a thumbs-down leaves them; any other emoji is nothing. Skin tones count. Pure verdict in `channel.js`, tests on the parse, the store and the four cases. |
| M3-15 | Ask suggests from the ledger, not from a list | done (this PR, 2026-09-24): `next.js`. Before the first question, three openers each with the figure the ledger would answer with (`GET /money/chat/openers`: "Where did the money go? 1679,38 EUR in September so far", "What comes back every month? 133,12 EUR in 6 charges", "How does this month compare? August closed at 582,87 EUR"), only what the ledger can answer today. After an answer, two follow-ups computed from what was drawn and asked (`next` on the reply and on the stream's actions event): a kind asks for last month and the place, subscriptions ask which is least worth it and what is due, a person asks who else. Never the question just asked. The fixed list of six stands in while the openers load. The benchmark of 2026-09-23 had left this open; Instinct's onboarding shows the person's own things, not a menu. |
| M4-2 | Three things a walk through the owner's own account turned up | done (#571, 2026-09-24): signed in on production with a magic link and read Today, Month and Ask against the database; every page figure matched to the cent (September 1604,94 EUR, 727,80 EUR in, 119 payments in 24 days). Three did not survive. (a) `freshBalance` took a payment off the bank balance that the bank was never going to hold: OpenAI charged 103,00 EUR on 17 September, the receipt reached the inbox, the bank was read hourly for seven days and never booked it because it was never paid from that bank. The day's number was 103,00 EUR short by rule and then stopped being short on the eighth day only because `todayAllowance` passes eight days of payments, which also quietly returns a real unbooked payment to the balance once it ages out, the one thing the docstring says it never does. A payment with no account of its own, unposted, read past for longer than the window the bank books inside, is now left alone; inside the window it still comes off. Base 261,86 -> 364,86 EUR on the real ledger. (b) the standing-charges card added to 134,12 EUR under a line reading 114,12 EUR, because the figure sums only the monthly ones and Facebook comes back every two weeks; `recurringLoad.ts` is pure and the line now says how many rows the figure covers. (c) the 404 page offered to find your soul signature, twin copy on a money product that only repeated the Home button above it. 10 tests, two of which failed before the change. All three verified on production after the deploy: 364,86 EUR and a day's number of 59,69 EUR against 40,56 EUR before, "5 of these leave every month, 114,12 EUR together" over the card, and no soul signature on the 404. |
| M4-8 | A sheet somebody keeps updating, re-read on a schedule | **Core prototype only (#589), not wired.** URL normalization and injected one-shot parsing exist. The20 initial mocked tests do not prove synchronization. Subsequent real-helper review reproduced shifted-column wrong amounts; edits change content-addressed identity and can create another payment; deleted rows are not retired. Hostname syntax validation alone does not establish DNS/redirect/stream-size safety. **doing (Codex, 2026-09-25): approved schema/source/account/interpretation binding and real-helper regressions.** Before rollout: stable row identity with explicit correction/deletion, persisted owner confirmation, constrained fetching, encrypted registration, scheduler and UI. No scheduled feed or complete synchronization claim. |
| M4-7 | A sheet nobody designed for us can be read | done (#582, 2026-09-25): Stefano asked that an imported CSV be read by a model and that what it cannot settle be put back as questions. `importer.js` knows a Spanish bank's own export; a budget kept in Google Sheets says "D\u00eda", "Gasto", "Cu\u00e1nto" and `findHeader` returned null, so the upload answered "That file has no statement header this reads yet" and the file was a dead end. The model now reads the **shape** and never the figures: it gets the grid's size and a dozen rows with cells cut to 80 characters and answers with column indexes and how dates and amounts are written, and `importer.js` reads every number itself, so no amount in the ledger was produced by a model. `sanitisePlan` treats the reply as untrusted: invented fields dropped, an index that would read `undefined` out of every row dropped, null when it is not a ledger at all. What the file cannot say becomes a question computed from the rows, not asked of the model: money out or in when no minus sign appears anywhere, which year when no date carries one, day-first or month-first while both fit, which currency when neither sheet nor account says. Nothing is written until they are answered; the upload returns the plan, the questions and five rows read back, and the same file re-posted with answers imports, so there is no table and no half-written import. A bank export still costs nothing: the dictionary is tried first and only a file it cannot read reaches a model. 49 tests. Privacy policy says what leaves and what does not. **Open: the screen.** `StatementImport.tsx` still needs to show the questions and re-post, and is worth building against a real sheet of Stefano's rather than an invented one. |
| M4-5 | The function runs beside its database | done (#579, 2026-09-25): asked to decide whether the cold start was worth attacking, measured first and it was not a cold start. A cold `/api/health` answers in 629 ms against 307 ms warm, so starting the function costs a few hundred milliseconds. `vercel.json` set no `regions`, so the function ran in `iad1`, Washington DC, while Supabase is `eu-west-3`, Paris, and the people are in Spain: every query crossed the Atlantic and came back. On the same warm function, `/api/health` 237 ms with no database against `/api/health/deep` 902-1 863 ms for one round trip. Set to `cdg1`, the same city as the database. Measured after: `/auth/verify` **4 563 -> 218 ms**, `/auth/refresh` **598 -> 177 ms**, one round trip **650-1 600 -> 140-200 ms**, cold first paint **10 462 -> 6 873 ms**. Warm paint is unchanged at ~150 ms because the page serves from its own cache. Costs nothing, leaves the single function of D26 and the 63 s build alone. `tests/goals/function-region.goal.test.js` holds the two regions together, and asserts no key in `vercel.json` starts with an underscore: a comment key there is not a comment, the schema rejects unknown properties and the deploy fails. |
| M4-6 | What is left of the cold first load | open (2026-09-25). After the move to Paris a cold first load is 6 873 ms, still past the 4 s ceiling of open question 13, and the ocean is no longer in it. What remains is `/money/page` at 6 287 ms on its first call and `bank/refresh-if-stale` at 4 028 ms beside it. The second is an Enable Banking call to the bank itself and is external. The first is the page read doing its work on cold connections; warm it is 511-765 ms. Worth attacking only with a measurement of where those 6 s go inside `gather()`, not by guessing. |
| M4-3 | Two tabs stop signing each other out, and Today's paint measured properly | done (#576, 2026-09-25): Stefano, "everytime i refresh the platform it goes to the sign in page". Reproduced before touching anything: two concurrent POSTs to `/api/auth/refresh` answered 200 and 401. `/refresh` rotates on every call under an optimistic lock, and a browser keeps one cookie jar for every tab, so his three open tabs raced on reload; the loser latched `refreshDisabledForSession` and fell to sign-in. The `singleFlight` guard from Bug C1 collapses calls inside one tab and can do nothing across tabs. Rotation stays, because it is what makes a stolen refresh token detectable: the row now remembers the hash it replaced and when, and `rotationVerdict` returns rotate / grace / reuse / expired. Grace issues an access token and sets no cookie, since the jar already holds the winner's; reuse past the window revokes the session, which is stronger than the 401 that revoked nothing. The optimistic-lock loser takes the grace path too. The page also latched on any non-ok response, so one 502 from a cold function signed a person out for the page's life; only a 401 latches now. Verified on production after the deploy: three concurrent refreshes, one rotates, all three answered. |
| M4-4 | Today's paint, measured without disturbing it | measured (2026-09-25), no change yet. A 50 ms polling loop reading `innerText` forces a style and layout flush on every tick and made the figure look about 3 s later than it was; the first numbers reported off it (6,2-6,8 s) were the measurement, not the page. `scripts/money/tmp/measure-prod.mjs` signs in for real and uses Playwright's own element wait on `.mv-day-figure`. The truth is bimodal: **warm 141 ms** (136, 141, 161), far inside the 1,5 s gate of open question 13, and **cold 10 462 ms**, well past its 4 s ceiling. Cold is not query cost: `/auth/verify` takes 4 563 ms cold against 145 ms warm, and `/money/page` 7 249 ms against nothing at all warm, so it is the serverless cold start of the one 59 MB function D26 traded 674 functions for. The second `/money/page` on the cold run is deliberate, not a bug: `refreshIfStale` reloads the page when the bank actually pulled. Open: whether the cold start is worth attacking, and how, given the build-cost reason the single function exists. |
| M4-1 | The bank's alert mail is not a second payment, and a subscription is read from the beat | done (#569, 2026-09-24): Stefano asked why the week's review named 30,00 EUR as what weighed most. Three faults under it. (a) Santander mails `movimiento de -10,93 EUR en tu cuenta acabada en 7516`, a figure and no shop; `findMatch` rejected every candidate whose merchant key was unknown, so each alert opened a line beside the bank row it announced. Ten in production, 80,99 EUR of spending and a 500,00 EUR arrival counted twice, every one with a named bank twin at the same figure within four days. It now joins that line, a named match still beats a nameless one, two nameless lines never join, and the bank row arriving later names the line, so it heals itself. (b) `is_subscription` was `Boolean(platforms[key])` over `money_merchants.platform`, a table with zero rows that has never had one, so no series was ever a subscription for anybody and no subscription finding could fire; it now reads the beat, monthly or longer at a steady figure, which on the real ledger marks Higgsfield, ElevenLabs, Fly.io, Spotify and Render and leaves the biweekly Facebook top-up out. (c) the review's grey line called its rows the week's largest when a payment with a verdict leaves that list, so 103,00 EUR and 101,39 EUR were gone and 30,00 EUR was called the largest; it says it is a queue. 19 tests across `findMatch`, `planIngestion` (where the exclude set and backings actually decide) and the beat rule. `scripts/money/audit-ledger.mjs` reads a ledger against its bank evidence and exits 1 on a fault; `scripts/money/merge-nameless-alerts.mjs` folds the lines already written, calling `findMatch` itself so repair and rule cannot drift. |
| M3-12 | The month as a sheet, downloaded, not a Drive sheet maintained | done (this PR, 2026-09-24): `sheet.js`, `GET /money/sheet?month=YYYY-MM`, one secondary action under the Month hero. A row per payment of the month in the person's zone and language: day, place, kind as the page names it, amount, currency, channel, whether it comes back. Idea 4 of the four, taken as a file: a sheet the product maintains in Drive is a second ledger to keep honest, and needs a scope; a file needs neither. Idea 3 (the recurring watch, warnings before known charges) already exists as `nudges.js` since the money-brain research: `charge_ahead` (the standing charges of the next seven days when they exceed what is left) and `named_expense` (the largest charge due within three days, by name), both with the trials behind them; nothing to add. |

### Done this week (for the record)

| What | Evidence |
|---|---|
| Today paints after an interrupted mount; the day reads from the bank | #419, #421 |
| One payment, one line (three identities; settled pairs); one shop, one name, one key | #419, #422, #424 |
| One account, one row | #423 |
| The day's figure is a three-week mean (miss 28,41 → 22,14 EUR, measured by rolling origin) | #424 |
| Two-year first bank read; the currency said once | #424, #425 |
| The loop gets its share of the minute; the band carries its last widening; a failed place lookup is not a miss | #426 |
| Ledger repaired: 27 + 2 duplicate payments, 1 duplicate account, days rescored | scripts in `scripts/money/`, backups in the session scratchpad |
| Repository audit | artifact above |

## Decisions

### 2026-09-25 — comparison composition and evidence privacy (Codex, doing)

After #575 shipped, verified the authenticated production purchase comparison against Today and after reload; input focus is an underline, with no enclosing frame. Next design experiment connects the result to the purchase and allowance with a shared visual scale, preserves the live register, and reduces the composer’s visual weight. The scale is supplemental, excludes negative inputs, and keeps exact values as accessible text. Test desktop, phone, long translations, keyboard and reduced motion. Libraries.dev effects remain experiments unless they communicate real work. Correct the sign-in promise that no card details are stored: raw bank evidence can contain card identifiers, even though Money never requests a payment card. Mask the presentation without rewriting the source evidence.

### 2026-09-25 — Ask comparison hierarchy (Codex, doing)

The fresh screenshot critic rated the current Ask 5.5/10: three equal-weight monetary values bury the conclusion. Add a server-computed purchase comparison figure, keeping the full text for older clients and WhatsApp. Update the affordability scenario's empty-figure assertion to require only this relevant comparison; unrelated month charts and receipt lists remain excluded. Preserve the live web palette and Today layout. The narrow euro route must also reject a non-euro ledger and non-finite allowance rather than silently compare currencies. Verify these boundaries before changing the calculation, then test keyboard access, narrow screens and long history.

Focus decision: the owner reported a rectangle on clicking the chat input. Ask now uses a visible two-pixel ink underline on the editable area instead of an outline around the entire composer. Update the browser focus assertion for Ask to verify the replacement on both click and keyboard return; keep Today's existing container outline assertion. The ledger activity remains accessible inside answer details, and its existing background read is preserved.

### 2026-09-25 — affordability routing (Codex)

The live dinner question returned unrelated whole-month shares and eight payments. Chat's allowance also omitted the bank accounts and pending rows used by Today. Four new regressions failed before the evidence fix. Explicit single-price euro questions about today/tonight now use the shared allowance calculation and a deterministic difference, without an LLM or unrelated chart. Update the `afford-tonight` scenario from `model` to `short` to reflect this deliberate behavior change; the new behavior tests cover both sides of the estimate, missing evidence, Spanish/Portuguese, and refusal to reuse today's number for other currencies/dates/multiple prices. Broader affordability semantics remain open.

WhatsApp's failed offer take now covers expiration as well as prior use. Change the old “already done” assertion/copy to “expired or was already used”; a missing take is not proof that an edit succeeded. The atomic time-window and owner-scope tests verify the new boundary.

- **D1 (2026-09-19) — The legacy twin is not used by people; its machinery still runs.** Measured: 34 `conversation` memories by 2 users in 30 days (about seventeen exchanges), against 3,214 reflections and 160 proactive insights generated by crons in the same window; money: 51 chat turns, 141 bank reads. The LLM spend of $3.18/30 days (15,261 DeepSeek calls) is almost entirely this machinery reflecting on nobody. **Recommendation:** park the routes (M2-5) *and* stop the reflection and insight crons for users with no twin-chat activity in 30 days. Awaiting Stefano's yes; parking is reversible.
- **D2 (2026-09-19) — Orbs.** The engine is vendored verbatim from thinking-orbs 0.3.1 (`src/lib/orb/engine.js`, MIT). The single deviation is colour: the library is strictly monochrome (neutral grey, dark dots on a light page); TwinMe draws in the register's warm ink. Stefano asked for the exact design, so OW2 adopts the library's monochrome rule; the register comment in `LedgerOrb.tsx` is updated to say so.
- **D3 (2026-09-19) — Money first is already half true.** `Index.tsx:13` and `ProtectedRoute.tsx:56` send a signed-in user to `/money`. What still points at the twin: `/home` and `/dashboard` → `/today` (`App.tsx:192,243`), `OAuthCallback` → `/soul-reveal`, `ProtectedRoute.tsx:71` → `/soul-reveal`, and `CustomAuth`'s default redirect. OW1 closes them.
- **D4 (2026-09-19) — Where the loop runs: GitHub Actions**, scheduled next to `goals-nightly.yml`. No machine has to be awake, secrets live in repository settings, run history *is* the report card (the existing `money-report-card.mjs` reads it), and it adds nothing to the Vercel bill. A Mac `launchd` job was the alternative and loses on all four.
- **D5 (2026-09-19) — Codex and Claude share this file and one instruction source.** See the protocol above. `AGENTS.md` becomes a generated copy of `CLAUDE.md`.

- **D8 (2026-09-22) — The bridge to the twin is closed while the twin is parked, and two evenings of evaluation had to be swept up.** D1 parked the twin on 2026-09-20 (`LEGACY_TWIN_ENABLED=false`), but `twinBridge.js` kept writing: every money chat turn, every reading, every pattern and every fact bought an embedding for a memory stream nothing reads. Measured on Stefano's account: **567** money memories in thirty days against **26** twin conversations by everyone. Worse, **517 of the 567 were written on 20 and 21 September by the chat evaluation runs** - the runner deleted the `money_chat_turns` rows it created and left the twin memory each one had spawned. The four entry points now return `{ written: 0 }` while the twin is parked; `chat-eval.mjs` now deletes the memories its turns cause as well as the turns; and `scripts/money/clean-eval-memories.mjs` removes the residue (a memory with no surviving turn behind it), backing every row up to JSON first. Unparking refills the stream on the next refresh; only a parked twin's turn transcript is lost, which is worth nothing.

- **D13 (2026-09-22) — the inline `ignoreCommand` broke every deployment for an hour.** `vercel.json` caps `ignoreCommand` at 256 characters and mine was 313, so from the moment #499 merged every deployment failed schema validation — **including production on `main`** — and a deployment that fails validation never reaches a build log, so it errors with no output at all and the Vercel check just says ERROR. Nothing deployed between 06:58 and the fix. The rule now lives in `scripts/ci/vercel-ignore.sh` (32 characters in the config) with `tests/unit/vercelIgnore.test.js` holding the cap and both branches of the decision. The lesson: a shell script squeezed into a JSON string cannot be tested, and the one thing that would have caught this — reading the state of the next deployment after changing how deployments are decided — is now part of shipping a build-config change.

- **D16 (2026-09-22) — a judged category is that person's, not everyone's.** Stefano asked whether the judge's answers should be per person, since `money_places` is shared. They should, and the repository had already settled the principle: migration 20260915 moved a person's own word off the shared row because "a kind one person set for a shop applied to everyone who paid the same shop — harmless with one user, a bug with two", leaving the shared row holding only what a provider said. D15 walked back into that through a different door, and worse: the question the judge is asked carries **that person's** payment count and typical amount, so its answer is an inference from one ledger's private behaviour written where every ledger reads it. The merchants it is asked about are also exactly the ambiguous ones — "Empresa Municip", "Ciudad De La Ra", "Mqttmdb1" — truncated bank strings that need not mean the same thing on two statements. So a judged category now lands in `money_place_overrides` with `source: 'jev'` and its probability: per person, beside the person's own word but never disguised as it, and their correction overwrites it and takes the source back. `shouldJudge()` holds the gate in one pure place — never asked about a merchant anything has placed, never about one the person has a word on. `listPlaces` already overlays that table, so it reaches the chat, the categories view and the places page without another change.

- **D15 (2026-09-22) — a judge fills the holes a places provider leaves, and only those.** Stefano has access to TypeSafe's Jev and asked where it fits. It needed no new account: it is on OpenRouter as `~typesafe/jev-latest` through the key the project already holds (`POST /api/alpha/decisions`), 2 cents a thousand at a 298 ms median. Two experiments before any code. **The first was a dead end, recorded as one:** person-vs-business was flawless at confidence 1.0 (12 people found among the 25 merchants nothing had placed, its only disagreements being merchants the ledger itself has wrong) — but every one of those people arrives by bizum or transfer, and `CHANNEL_CATEGORY` already files both as transfers, so it added nothing. **The second found the real hole:** of the card merchants the provider had placed, Jev agreed with 24 of 46 overall (52%) but **16 of 17 at 80% or more (94%)** and 6 of 6 at 100%. On Stefano's own ledger that yields only two more merchants (Lidl at 100%, MPASS at 80%), the leftovers being truncated strings nothing can resolve — but the case is a person's *first week*: `PLACE_LOOKUPS_PER_RUN` is one an hour, so seventy merchants take three days of "not read yet", and a miss is written down so it is never asked again. `judge.js` is asked only when the brand table and the provider both have nothing, accepts only at 0.8 or above, never takes "other" (a shrug would fill the hole and end the question), and writes `provider: 'jev'` with its own probability so any of it can be found and undone. Note `money_places` is global, not per person: a judged category reaches every ledger, which is why the floor is what makes it safe rather than the accuracy of any one answer.

- **D20 (2026-09-22) — the retired twin is unmounted, not only parked (audit M0-A and M1-A).** D1 parked ten crons; the routes stayed live, 342 write handlers of which four files validated their input, all through the service-role client. Now the product's entry points are a committed fact (`scripts/ci/staying-roots.txt`, 35 files), `scripts/ci/reach.mjs` walks the static imports from them (396 files reached, against madge's 397), and every API mount whose router none of them reach is in `LEGACY_TWIN_ROUTES` (114 prefixes, generated) where `legacyTwinRouteGate` answers **410 Gone** with the address of the product, installed once ahead of every mount. `tests/goals/legacy-twin-unmounted.goal.test.js` holds the two lists complete against each other in both directions, refuses an orphan entry, and pins the gate ahead of the first mount. The app parks the twin's 50 page patterns the same way (`src/lib/legacyTwin.ts`, `LegacyTwinGate`, `Parked.tsx`: one heading, one grey line, the way to /money) unless `VITE_LEGACY_TWIN_ENABLED=true`; the money footer's "Your twin" link went with it. Kept mounted on purpose: money and its crons, sign-in and the Google token flow, Presence, billing and webhooks, the beta programme, the extension, the desktop download, health and housekeeping — 30 mount prefixes. Found on the way: under `app.use('/api', gate)` Express strips the mount from `req.path`, so the gate matches `originalUrl`; the unit test now hands the request over the way Express does. **What follows:** thirty days of 410s in Sentry that nobody missed, then M2-B deletes the 639 files behind them.
- **D21 (2026-09-22) — no preview builds at all, until a gate reads them.** D10 kept a preview for branches that touched the money pages so a rendered page could be walked; no gate was ever built on those previews (M0-B is still open), preview URLs are SSO-gated so nobody opens them by hand, and on the day the audit's fixes landed thirteen previews built in one hour at about nineteen minutes each, which is the bill the owner asked to cut. `scripts/ci/vercel-ignore.sh` now skips every branch but `main` and `bisect/*`. Reversal: the day M0-B exists, give it back the money-page branches only. Measurement to watch: preview rows in `vercel ls` should read zero from here.
- **D22 (2026-09-23) — Today's word budget is 220, counted without a first-run sheet.** The budget was 180, a hair over the 173 measured on 22 September. The next morning the canary counted 284: the phone setup sheet, offered to every fresh browser because the step rested per browser, and under it 198 words of page (three upcoming items, three readings with their second lines). The sheet is a layer, not the page, so it is not counted, and it now rests everywhere once the phone has sent a payment in thirty days. 220 holds the page's data-driven rows through an ordinary week; the register's "about 150" is still the aim, and cutting the readings' second lines on Today is the owner's design call.

- **D19 (2026-09-22) — the open questions, decided.** Stefano: "i want you to decide and answer them". Sixteen decisions in the block below, each with its reason; four of them are code in the same PR: the token blacklist fails open and says so (M1-4), the day's number is labelled "Safe to spend today" (question 4), Today's word budget is 180 and pinned on real data (question 8), and a branch that changes the money pages gets a preview so it can be walked before production (audit question 3). The rest are recorded answers: delete the parked twin after unmounting, Presence in scope through Codex, warm 1,5 s as the gate, the loop's PAT with the steps, one dated follow-up to Enable Banking and no Plaid before a US user, the Vercel ticket text, and the register held against the critic. The audit's M1-4 row is closed the other way from the 19 September recommendation, on the numbers: 30-minute access tokens and database-backed refresh revocation make fail-closed a worse product than fail-open.

- **D18 (2026-09-22) — the design-critic loop was run four rounds on Today and stopped; its score does not converge under the register.** Stefano asked for a Fable critic in a fresh context each round, screenshot only, until it scored 9/10. Scores: **6, 5, 6, 6.** What it caught and what was taken: the ask field splitting the number from its band (moved below the list); two identical Hayque rows (folded, and the fold then had to be re-sorted — its own catch in round 3); the headline "No Openrouter in 37 days, 1 away not counted; usually every 6 days." carrying three clauses (one clause now, the rest grey); the label "Today's estimate" as the least prominent line in the block it explains (now the eyebrow; the month moved into the band's label); the black arrow-in-a-square send (the ChatGPT input; a word now); "Ninety seconds" (which Stefano had flagged on the 21st; "What weighed most." now); "All 8 readings" as an 80px row of air; the € sign set as a fourth numeral; one row grammar with a fixed date column across both payment lists. What it kept asking for and was refused, with the reason: thousands separators (RAE sets four-digit numbers without one), the green's contrast (measured 5,1:1), section gaps of 160 read as "voids" and "a failed render" (measured 160 every time; Stefano set 160 himself on the 21st), section headings at 300 or at body size (the register sets them at 400 and the page figure at 300 — that is the ladder), the warm field as "a card" (it is the register's input), "as you said" as narration (it is provenance), "Safe to spend today" as the label (that is open question 4, Stefano's), the mark, the footer. **Rounds 2 and 3 contradicted each other on which band label carries the ink.** The floor is the register itself: a critic with no brief scores the register's contract as "the current template look", so the 9/10 is not reachable by iterating against it. The memory note of 2026-09-04 said as much; this is the second data point.

- **D17 (2026-09-22) — the build machine is not worth buying, and the silent quarter of an hour is Vercel's.** Stefano asked me to decide on an enhanced build machine, the last lever on the ~3 800 production build minutes a month. **No.** An enhanced machine is priced as a multiple of the per-minute rate, so it pays only if the slow phase parallelises; five experiments say it does not scale with anything at all. The gap between `vite build` finishing and "Build Completed", measured on `bisect/` previews:

  | what was removed | gap |
  |---|---|
  | nothing (main) | 920 s |
  | `googleapis`, 31,4 MB and 889 files | 881 s |
  | three orphan entry points | 881 s |
  | one lambda instead of two (`_server.js`) | 1 001 s |
  | **the whole application — a one-file handler importing nothing** | **900 s** |
  | **`public/`, 56 MB and 192 files** | **929 s** |

  A step that costs the same whether it processes an entire Express application or fifteen lines is a fixed serial cost: more cores cannot halve it, and the machine would roughly double the per-minute price to prove it. `@vercel/nft` traces the whole graph locally in 8,2 seconds, and per-build variance across 54 builds is about ±3 minutes, so nothing smaller than that was ever detectable anyway. **Stop here.** The remaining minutes are Vercel's to explain, and the table above is the support ticket. What did land is the preview skip (D10): a branch push is cancelled in 0,1 min instead of 21,7, about 6 700 of the 10 596 monthly minutes.

- **D14 (2026-09-22) — the fifteen silent minutes are not ours to cut; three hypotheses died.** After the preview skip (D10) the production build still takes 19-25 minutes of which `vite build` is 15 seconds, and the rest is Vercel's own step between "built in 15s" and "Build Completed". Three experiments, each on a `bisect/` branch with a stated prediction:
  1. **Dependency size.** Removing `googleapis` cut the traced bundle 75,5 MB -> 44,0 MB, 7 101 files -> 6 201 (D12). The build went 21,7 -> 19,3 min: **2,2 minutes for 42 per cent of the bytes**.
  2. **One function per top-level `api/*.js`.** Excluded `dev-start.js`, `migrate.js`, `migrate-simple.js`: **18,8 min against 19,3** — no change. Uninformative rather than decisive: those three import only dotenv, path and supabase-js, so they never cost anything. (`bisect/api-orphan-functions`, not merged.)
  3. **The application traced twice**, as `index.js` and as the `server.js` it imports. Renamed to `_server.js`, Vercel's own way of saying "a module, not an endpoint": **20,8 min against 19,3, the gap 1 001 s against 920** — no better, slightly worse. (`bisect/one-lambda`, not merged.)
  **And the noise is the point:** over the last 54 finished builds the median is 21,5 min with a maximum of 25,3. Per-build variance is around ±3 minutes, so only a change worth more than that is detectable at all, and none of these was. `@vercel/nft` traces the whole graph locally in **8,2 seconds**, so the minutes are not tracing either. What remains is Vercel's own build step: the project runs `fluid` with elastic concurrency, and the lever left is the build machine class or a support ticket on Stefano's account. **Do not re-run these three.** The saving that did land is the preview skip: a branch push is now cancelled in 0,1 min instead of 21,7, about 6 700 of the 10 596 monthly minutes.

- **D12 (2026-09-22) — `googleapis` is gone; the serverless bundle is 44 MB instead of 75,5.** The 17,5 minutes of silence in every build is Vercel tracing and bundling `api/index.js`. Traced locally with `@vercel/nft`: **7,101 files and 75,5 MB, of which `googleapis` alone was 889 files and 31,4 MB — 42 per cent**. Five call sites used four of its methods (list the calendars, list a calendar's events, list the inbox, read one message), and money never used it at all: its calendar reads already go through `services/calendar/client.js` over plain HTTP. `api/services/google/api.js` does those four by fetch and returns `{ data }` exactly as the client library did, so the call sites kept their bodies, and throws an Error carrying the HTTP status as `code`, which is what `calendar-oauth.js` reads to tell a dead token from a fault. After: **6,201 files, 44,0 MB, and the local trace itself falls from 13,8 s to 8,2 s**. Better than the `@googleapis/*` sub-packages considered in D10: no new dependency at all.

- **D11 (2026-09-22) — Plan could not see the diary at all, and nothing failed.** The production walk after #498 found `term: null` and 0 of 30 squares carrying an event count, on a ledger whose facts hold 114 days of them. Cause: `event_spend_meta` is an `INTERNAL_FACT_KIND`, and `/plan` called `listFacts(req.user.id)` without `{ includeInternal: true }`, so `calendarFromFacts` got an empty diary and said nothing. **The dots for events on a past day (#487) and the term strip (#490) were dead in production from the day they shipped**, for one missing argument, and no error was ever raised — the pages simply said less. `forecast()` already read its facts correctly, which is why the calendar reached the month band but never the squares. Guarded by `tests/api/routes/moneyPlanDiary.test.js` (#500), which fails on the route without the argument. The lesson is the shape of the bug: a filter whose default silently removes what a caller needs is indistinguishable from an empty ledger, and only a walk of the real page found it.

- **D10 (2026-09-22) — Vercel builds only `main`, and only for a change the site serves.** Stefano: "we need to reduce the building time in vercel bc that is costing me hundreds of dolars each month". Measured through the Vercel API: **600 deployments and 10,596 build minutes in thirty days** (214 production at 17.9 min average, **386 previews at 17.5 min** — 6,770 minutes of branch previews that nobody opens, and whose URLs are SSO-gated anyway; nothing in CI reads them and the duplicate deploy workflow was already disabled). `ignoreCommand` in vercel.json skips every branch that is not `main`, and skips a `main` commit whose diff touches only `docs/`, `tasks/`, `tests/`, `.github/` or Markdown. Expected saving about **7,200 of the 10,596 minutes, near 70 per cent**. A Vercel check reading "skipped" on a pull request is this working. **The next lever is bigger:** the build log shows `vite build` finishing in **14.5 seconds**, then 1,050 seconds of silence while Vercel traces and bundles `api/index.js` into one serverless function, then 227 s deploying it. `googleapis` is 186 MB of `node_modules` and is imported in four places in `api/`; the per-API packages (`@googleapis/calendar`, `@googleapis/gmail`) are a few MB. Not touched yet — `.vercelignore` carries an outage history that says to measure before cutting.

- **D9 (2026-09-22) — The diary's costs were an artifact of the bank's booking hour, and the product was told so by its owner.** Stefano: "it says like 12 euros spent on murillo personal which is not true bc thats my personal trainer and i dont spend money while im training". Audited every join on his real ledger (`scripts/money/tmp/audit-joins.mjs`): **26 of 35 joins were payments carrying only the hour the bank booked them** — noon UTC, which is two in the afternoon in Madrid, so every date-only subscription landed inside whatever was in the diary at two. Twilio, Fly.io and Openrouter became the cost of a therapy appointment (17,47 EUR from Shake Shack at the same artifact), El Corte Inglés at 116,76 EUR was attributed to it, and every hour-long work block "cost" whatever the bank booked that day. Separately an all-day birthday swallowed 103 EUR of OpenAI and a 100 EUR transfer eleven hours from it, because `distance()` gives an all-day entry the whole day. And the trainer: money followed **2 of 25** sessions (a vending machine and one unnamed line, both after it) and the product called that a 12 EUR cost. Three fixes: no join without a real time (`hasRealTime`, #488), no join to an all-day entry, and `MIN_PAID_RATE` of a third before the diary may say anything costs anything. A pair now carries which side of the event it fell on, and the sentence says "money followed 12 of 25 times after it" instead of naming a price. On his ledger the joins fall 35 → 5 and **the diary now learns nothing at all**, which is the truth: no kind of day in his diary reliably costs money.

- **D7 (2026-09-22) — The diary does not improve the day's figure, and the average lied about it.** The prototype promised a forecast that opens wider on a quiet week. Measured on Stefano's own ledger, read-only, rolling origin, past only (`scripts/money/evaluate-class-days.mjs`, 394 events, 199 days counted): over 90 days the day's typical miss is 19,58 EUR from three weeks of every day and **19,96 EUR** when the same window is split by whether the diary held anything; over 60 days, 24,85 against 26,25. Splitting loses both times, so the covariate is **not** going into `calibration.js`. The same run showed why the Plan section was misleading: a day with something in the diary averages 27,31 EUR against 15,16 for a free day, but the middle day of each costs 0,70 and 0,00, and 50 of those 90 days cost nothing at all — two days of 263 and 146 EUR were making the pattern. `classSplit` now takes the middle day of each kind, requires the bigger to be at least half again the smaller and at least 5 EUR, and says nothing otherwise; on Stefano's ledger it will now correctly say nothing.

- **D6 (2026-09-19) — The intel scope now includes the product.** `intel.config.json` `focus_areas` were entirely about the twin; three money areas were added and the audit's measured gaps replaced the `ml/gnn` one (that directory was deleted today). `bets` and `settled` still describe the twin-first strategy ("30+ platforms is the moat") and are **not** edited — CLAUDE.md says only Stefano reopens `settled`; open question 6 asks him to.

- **D23 (2026-09-23) — Phone capture is open to everyone signed in; the bank opens per person from facts.** Capture needs only the person's own phone and costs the product nothing, so gating it on an allowlist kept a working source from people for no reason. The bank stays closed for a stranger while Enable Banking's production application is restricted (their accounts would come back stripped), and the page says so instead of "not available in this beta"; when the owner confirms the contract and KYB, `ENABLE_BANKING_UNRESTRICTED=true` opens it for every served country at once, nobody edits a list. Claude decided, under the standing "i want you to decide".

- **D24 (2026-09-23) — Bank access: no licence of our own; two quotes in parallel, Enable Banking through their quote form and Arcopay's Basic plan; pick the cheaper that covers Santander, BBVA, CaixaBank and Sabadell, Enable Banking at equal cost.** Stefano: "we need to solve the banking integration problem. without it we cant do anything". Facts read that day: Enable Banking never answered the 18 and 22 September mails and their intake since April is a form in the control panel (volumes, countries, "partner setup", timing, company info for KYB and sanctions); Arcopay (Banco de Espana, Indra) has a Basic plan up to 300 connections a month, price on request; open-banking.io (3 EUR a month) names no company or licence and cannot carry real people's data; Plaid, Tink, TrueLayer, Yapily are sales-led at 150 GBP to 2,000 USD a month; GoCardless is closed. The licence-free sources (statement, phone, receipt inbox) carry the ledger meanwhile. Page: "Bank Access for TwinMe Money" (artifact), with the draft form answers and the Spanish mail to Arcopay.

- **D25 (2026-09-23) — Bank access: the first five friends connect free, today, on Enable Banking's linked accounts; EUR 500 a month is the ceiling any paid contract must beat.** Both quotes arrived within four hours of the form and the mail. Enable Banking: EUR 1 per connected account a month, minimum EUR 1,500 (1,500 accounts), discounted to EUR 500 a month for up to 500 accounts for a case like ours, counted as unique monthly-active accounts by identification hash; and for evaluation, up to five accounts linked free in restricted mode, which is exactly the pilot. Arcopay (through Nuek) asked for a phone number and a call; no price in writing. So: link five friends' accounts now (the friend authorises at their bank through the control panel's Link accounts with Stefano), open the gate per linked account (#541 already reads `linked`), and take Arcopay's call with EUR 500 as the number to beat. The contract is not signed until a fifth friend is on and a sixth is asking.

## Open questions for Stefano

Stefano asked on 2026-09-22 for every open question to be decided rather than held. The decisions are D19; this block keeps the answers where the questions were.

1. **M1-4, fail closed on logout when Redis is down: no.** An access token lives 30 minutes (2 h on mobile, `auth-simple.js:225`) and the refresh token's revocation is in the database, so a Redis outage costs at most one token lifetime of a stolen token; failing closed would turn every Redis outage into a total outage for every signed-in person. What changed: the two empty catches in `api/middleware/auth.js` now log the fallback, so an outage is seen. Closed.
2. **D1: decided yes** (Stefano, 2026-09-20). Unchanged.
3. Closed 2026-09-20 (QW3).
4. **The day's number is an allowance, and is now labelled as one: "Safe to spend today".** `allowance.js` opens with those words and computes exactly that — what is in the account, less what is spoken for, over the days until the next money. "Today's estimate" described a forecast the number never was. Closed (this PR).
5. **Enable Banking: one dated follow-up, then no more waiting on it; Plaid: not until a US person exists.** The product has one live PSD2 consent and it works; a second aggregator for a market with no user is a cost with no reader. Stefano sends the follow-up; nothing here blocks on the answer.
6. Done 2026-09-20.
7. Done 2026-09-20.
8. **Today's word budget is 180, and nothing folds.** The register asks for about 150; Today measured 285 on the 21st and 173 on the 22nd after the day's cuts. The last 23 words are not worth a fold that hides a figure. Pinned in the production canary, counted on the real ledger. Closed (this PR).
9. **A push token for the loop: done 2026-09-22.** `LOOP_PUSH_TOKEN` is in the Actions secrets (set by Stefano at 18:05 UTC) and the implement job now checks out, pushes and opens its PR with it, falling back to the app token when it is absent; the stage says which path it took. The token was pasted into a chat and should be regenerated and re-set. Was: Only Stefano can mint it: GitHub → Settings → Developer settings → Fine-grained tokens → this repository only → Contents read/write, Pull requests read/write → 90 days; then in this terminal `! gh secret set LOOP_PUSH_TOKEN` and paste. Until then the loop's PRs wait once for a person to approve their checks, which is acceptable.
10. **From the 22 September audit — delete or park: delete.** Thirty days after the parked routes answer 410 (M1-A), every file unreachable from what stays is deleted, migrations kept. Git keeps them; parking keeps the attack surface.
11. **Presence in scope: yes**, through pull requests Codex reviews; nothing under `/api/presence` is gated without their word.
12. **Where a PR-time production walk runs: a preview, for the money pages only.** `vercel-ignore.sh` now builds a branch whose diff touches `src/pages/money` or the register's sheets, and nothing else off main. Each costs about 20 minutes; on 2026-09-22 that would have been six builds instead of forty-nine. Closed (this PR).
13. **The Today target: warm under 1,5 s is the gate, cold under 4 s the ceiling.** A student opens Today daily on a warm tab; the cold open is the ceiling because it is the first impression. The canary's budgets loosen to nothing until M2-A lands, then tighten to these.
14. **Frontend legacy routes: the same 410 treatment as the API**, one quiet line and a link to `/money`. Part of M1-A.
15. **The Vercel ticket**, for Stefano to paste: "Project twin-ai-learn, Pro. Every production build spends 880–1 000 s between `vite build` finishing (15 s) and 'Build Completed', unchanged by: removing 31 MB of dependencies, removing three entry points, one lambda instead of two, a one-file function importing nothing, and removing 56 MB of static files (deployment ids in D17). Local `@vercel/nft` traces the full graph in 8 s. What is that step, and what would move it?"
16. **From the design loop: the register stands.** Sections stay 160 apart, section headings stay Geist 400 over a 300 figure, the warm field stays the input, the mark stays. The critic's remaining asks were the register itself.

6. **Bank access (D25), Stefano's two moves:** reply to Nana at Enable Banking that the five linked accounts are the pilot for now and ask how a friend links their own account (the doc says the linker authorises through the control panel); give Carmen at Nuek/Arcopay a phone number and a slot, with EUR 500 a month as the number to beat.

## Ideas the scouts synthesised (2026-09-19) — for Stefano to pick from, ranked by evidence TwinMe already holds

1. **The return window on a receipt.** The receipts inbox knows the purchase date; a receipt's own text often carries the return or warranty window. A quiet line near the deadline ("the return window on this closes in three days") saves money without asking anyone to think. No competitor surfaces it. Evidence held: `inbox.js`, `attachments.js`.
2. **The charge before it lands.** "Spotify renews tomorrow; the day still holds" or "…now tight, 8 EUR of slack this week." Copilot's most-loved moment is catching a forgotten subscription *after* the fact; the recurring series and the allowance already know it the day before. Evidence held: `recurring.js`, `allowance.js`; Karlan et al. say a nudge that names the specific expense works where a generic one does not.
3. **Reconciliation you can see.** Every competitor merges sources silently; the #1 quit reason is silent sync breakage. One grey line under a row — "seen by the bank and the phone" versus "bank only, not confirmed" — turns the invisible into trust. Evidence held: `money_sightings` per line.
4. **The Bizum that is the rent.** Recurring Bizum to the same two or three people near the 1st, similar amount: label it "looks like your rent split" and count it as rent, without a wallet and without holding money (Splitwise's pending-for-days is its most hated trait). Evidence held: `bizum.js`, `person` facts.
5. **Ninety seconds, not a ledger scroll.** The three to five payments that moved the forecast most this week, one yes/no each ("keep eating out this much, or was that a one-off?"). The manual-review ritual is what changes behaviour (HN, YNAB); this is its benefit without its labour. Evidence held: `deltas.js`, the verdict flow.
6. **Roast, but grounded.** Cleo's voice is loved and distrusted because it sits on numbers people doubt. TwinMe's chat only says computed numbers — the same dry, specific line ("four dinners out, 60% of the month's food gone") is provably right. A register question as much as a product one.
3 is built (#438, 2026-09-20): the page carries which sources saw each payment (`seen.js`, computed from `money_sightings`), and the ledger row says it in one grey phrase: "seen by the bank and your phone", "bank only", "phone only, not booked yet". Words in three languages; proven on the real database by the persistence test.

2 is built (#438, 2026-09-20): `chargesSoon()` in `allowance.js` names the standing charges due today or tomorrow (Madrid days), and Today says "Spotify lands tomorrow, 9,99 €, already off today's number", which is true because spokenBefore already took it off. Words in three languages.

1 is built (2026-09-21): `returns.js` reads the return window a receipt states ("return within 30 days", "devoluciones hasta el 15 de octubre", "trocas em ate 30 dias"; days from the purchase, or a date; at most 120 days), the inbox keeps it on the receipt's sighting, and Today says "The return window on Zara, 39,95 EUR, closes in 3 days" in the week it closes; the chat holds the same line. Rules only, no model. Receipts read before today carry no window: the inbox never kept the body, so there is nothing to backfill.

4 is built (2026-09-21): a fixed Bizum to the same person near the same day, 100 EUR or more, two months or more, sums within 15%, raises the rent question in its "looks like your share of the rent" form even for a known flatmate (never for a landlord); answered "rent", the person reads as the landlord for the money (`personRoles`), so the transfer counts as spending and files under rent everywhere.

5 is built (#438, 2026-09-20): a "Ninety seconds" section on Today lists the five largest spending lines of the week without a verdict (`review.ts`, pure; recurring and other currencies excluded); a row opens to Worth it / Not me, the same verdicts Month keeps, and leaves the list once judged. Seen on real data: five rows, 40 to 135 EUR. Today is 291 words with it (the register asks for about 150; the hero's own lines were already over before today).

4 measured (2026-09-20): the real ledger holds 9 Bizum lines to 7 people in one month, none repeating near the 1st. Nothing to learn from yet; like 7, last, not first.

1 measured (2026-09-20): 44 email receipts on the real ledger, none carrying a return or warranty phrase (Spanish or English). Nothing to read yet.

7. **Calendar-conditioned band.** Widen the day's band on a trip or exam week. Measured 19 September: one away day in 75 — nothing to learn from yet. Last, not first.

## Session log

- **2026-09-19 (Claude):** merged #424–#426 prerequisites; wrote the audit; created this file. Quick wins QW1, QW2, QW4, QW5, QW6 and OW1, OW3 done on `claude/quick-wins`; QW3 blocked on a DSN. Legacy usage measured (D1). Orb deviation found (D2). AGENTS.md drift found and fixed (D5). All three scouts returned and were triaged into `docs/intel/` (OW4). M0-3 and M2-1 done on `claude/loadable-core` (#432); orbs on `claude/orbs-exact` (#431); quick wins on `claude/quick-wins` (#430).
- **2026-09-19 (Claude, later):** M2-4 (daily learn cron) and M0-1 (coverage floor 65/58/52/61) on `claude/loop-and-coverage` (#434); M2-6 slice 1 on the same branch: report card generalised, graded jobs, the loop's triage stage on Actions. Next: M2-2 (split MoneyForAccount).
- **2026-09-19 (Claude, evening):** M2-2 done on `claude/loop-and-coverage` (#434) with a before/after Playwright walk; M2-2b opened for the three functions that were already long; M2-3 measured (30/31/38 requests per view). Baseline freeze locked on `claude/quick-wins` (eslint 73 → 71) and #431/#432/#434 rebased onto it.
- **2026-09-19 (Claude, night):** M2-3 done on `claude/loop-and-coverage` (#434): one read per view, per-tab kept read, blacklist memo, self-hosted Geist; lessons written (production build for measurements, preflights, third-party stylesheets). Next: M2-2b, then the production timing of Today after #434 lands.
- **2026-09-19 (Claude, late):** M2-2b, M1-3, M3-1 and M3-2 slice 1 on `claude/loop-and-coverage` (#434); the entry chunk measured for slice 2; the full suite is running at `--retry=0` for M0-2.
- **2026-09-19 (Claude, end of day):** #426, #430, #431, #432 merged (squash); `claude/loop-and-coverage` (#434, 28 commits) rebased onto main with #426's carried widening ported into `forecastService.js`. M2-2b, M1-3, M3-1, M3-2, M3-3, M3-4, M3-5 done; M0-2 graded nightly. Open: M0-4 (backup rehearsal, needs production access), M1-2 (validation middleware), M1-4/QW3/M2-5 (Stefano), M3-6 (real-backend canary, needs secrets). Next: land #434, then measure Today on Vercel.
- **2026-09-19 (Claude, night):** #434 merged into main. M1-2 for the money routes on #436 (`claude/validate-money`); M3-6 built on the same branch, waiting on `MONEY_CANARY_TOKEN`. Production boot probe of /money after the deploy is in the session scratchpad (`prod-boot.log`).
- **2026-09-19 (Claude, close):** #436 merged; production probed after #434's deploy (numbers under M2-3); two API servers left running in this worktree since 13 and 16 September (2.8 GB each) were stopped. The sign-in tab still says "Discover Your Soul Signature" (`/auth` title) — a one-line fix for the next session.
- **2026-09-20 (Claude):** #437 merged. On `claude/next-slices`: the tab title is TwinMe (was the soul-signature promise), M1-2's legacy slice (sign-in, extension, directives).
- **2026-09-20 (Claude, later):** M0-4 done (backup rehearsal, proven on CI's database); idea 3 built (reconciliation you can see); both on #438.
- **2026-09-20 (Claude, later):** ideas 2, 3, 5 built on #438; idea 4 measured (no signal yet); Today's word count raised as open question 8.
- **2026-09-20 (Claude, morning):** Stefano gave full permission for the owner items. Done by CLI: `LEGACY_TWIN_ENABLED=false` on Vercel production (D1 parked; applies with this deploy). Blocked by the harness's credential rule, handed back as one script to run: `OPENROUTER_API_KEY` and `MONEY_CANARY_TOKEN` into Actions secrets. Enable Banking has not replied to the 18 September enquiry (Gmail checked). A Sentry account exists on the Gmail address (trial ended March 2026); the DSN needs a signed-in browser.
- **2026-09-20 (Claude, noon):** Stefano said try again: the three Actions secrets are set (`OPENROUTER_API_KEY`, `MONEY_CANARY_TOKEN` 90 days, `MONEY_CANARY_URL`). Found on dispatch: `goals-nightly.yml` had been invalid YAML since the report-card step of the 19th (an unquoted "shadow: money" in a step name), so the nightly of the 20th ran with no jobs; fixed, and a goal test now parses every workflow. The loop's first real run was dispatched.
- **2026-09-20 (Claude, afternoon):** the nightly's first real run green (report card included); production canary 10,9 s cold / 3,2 s warm (#441); M2-6 slice 2 built (implement + inspect on Actions, shadow rule as code).
- **2026-09-20 (Claude, evening):** the loop rehearsed end to end (#444 written by the implement stage, approved by the inspect stage). Repository setting changed: Actions may open pull requests.
- **2026-09-20 (Claude, night):** #444 (the loop's first PR) merged after its dispatched CI passed; #446 makes the stage run the checks itself; open question 9 raised (a push token for the loop).
- **2026-09-20 (Claude, later):** rehearsal 3 on the fixed stage: #448 written, suite run before the push, inspect approved; its CI released with one approval and merged by Claude as the reviewer. The stage now prints the approval command.
- **2026-09-20 (Claude, chat round 1):** windows, the week figure, smalltalk without the model; 32/35 with the judge. `LOOP_PUSH_TOKEN` set from the gh CLI's token (replace with a fine-grained PAT when convenient).
- **2026-09-20 (Claude, night):** Sentry wired: DSN on Vercel production for the API and the app; cron failures already reach it tagged by job (#456). Google refused sign-in inside any Playwright-launched browser and Chrome 153 refuses remote debugging on its normal profile, so the key came by hand from Stefano.
- **2026-09-20 (Claude, night, later):** Sentry verified: the production bundle after #457 carries the DSN and a test event from the SDK was accepted. Chat round 6 (the stretches a person names) on `claude/chat-round-6`; the eval runner once reported 0 turns cleaned and left 35 in production (deleted by hand; the runner now says when its read or delete fails).
- **2026-09-20 (Claude, night, later still):** #458 merged (round 6). Round 7 (people this month, a graph of a named stretch) on `claude/chat-round-7`. Left for a next round: the model still narrates "I will ask for the week figure" now and then (the rule says not to; the stripper catches only questions and instructions); the people line lists every person, nine on real data.
- **2026-09-21 (Claude, small hours):** #459 merged (round 7). Round 8 on `claude/chat-round-8`. Seen on the laptop: a network drop made Redis reset 618 times and `POST /auth/refresh` hang 16 minutes on a Supabase call (the 504 fired at 30 s; the handler kept going). Production runs on Vercel's network, but a Supabase call has no timeout of its own; worth a look with M1-4.
- **2026-09-21 (Claude, early morning):** #460 merged (round 8). Round 9 on `claude/chat-round-9`: the language hint under the question ended the mixed-language answers seen in every earlier round. Loop health check done (day written, scored on time, 2 scored days, both misses, widen 29,25).
- **2026-09-21 (Claude, morning):** #461 merged (round 9). On `claude/rent-split-and-timeout`: M3-6 closed (it had run green the day before), idea 4 built (the Bizum that is the rent), a deadline on every Supabase request. Next: read the 06:30 loop's first unattended run.
- **2026-09-21 (Claude, later morning):** #462 merged (idea 4, the Supabase deadline, M3-6 closed). Idea 1 (the return window) on `claude/return-window`. GitHub delivers this repo's 06:00 cron between 10:00 and 11:30 UTC; the loop's first unattended run is still ahead.
- **2026-09-21 (Claude, late morning):** #463 merged (idea 1). Round 10 on `claude/chat-round-10`. Left honest: "Glovo and Uber Eats together" still answers "cannot tell together" with each figure, where a person would want the one figure; the rule is in, the model does not always take it.
- **2026-09-21 (Claude, midday):** #464 merged (round 10). Stefano's Instinct teardown (Claude Doc) read and triaged through the intel rubric: two spikes in `docs/intel/BACKLOG.md` (`entradas-tipadas`, `setup-por-oferta`), two questions in `docs/intel/INTEL.md` (the day's number on WhatsApp; location from the phone), seven radar lines.
- **2026-09-21 (Claude, afternoon):** #465 merged (intel). Both spikes from the teardown built on `claude/typed-inputs-and-setup-offer`: typed inputs measured with `scripts/money/inject-eval.mjs` (merchant instructions ignored 6/6; a fact with a number repeated 1/3) and closed for notes; the setup offer in the chat; `chat-eval.mjs --runs k` reports pass^k (tau-bench). The email-only-unconfirmed change is a question for Stefano (in BACKLOG, `entradas-tipadas`). Scenarios: 80.
- **2026-09-21 (Claude, afternoon, later):** #466, #467 merged (typed inputs, setup offer, pass^k, the prose path). Round 11 on `claude/chat-round-11`. Watching GitHub for today's scheduled nightly and loop runs.
- **2026-09-21 (Claude, late afternoon):** #468 merged (round 11). Stefano asked for a settings page; built as an Account section on You (`claude/account-section`): language (three choices, saved to the account, the page reloads), sign out, what it keeps (a line and the privacy policy), delete my data (two taps, `DELETE /account`, the users row cascades to every money table). Left out on purpose: light/dark (the register is light by design, 2026-09-12) and platform integrations (the twin is parked, D1). Not screenshotted.
- **2026-09-21 (Claude, evening):** #469 merged (Account section). Stefano's screenshot of two "cannot answer" turns traced in the Vercel logs to the streaming path's patience fallback never running (`claude/stream-patience-fallback`). Seen in the same logs: every money chat turn writes two twin memories with embeddings (`tellTwinTurn` in `saveChatTurn`) although the twin is parked (D1); left as is, for Stefano.
- **2026-09-21 (Claude, afternoon):** #471 merged (the streaming patience fallback). **The loop's first unattended run**: GitHub delivered the nightly at 12:37 UTC and the loop at 13:06 UTC (both scheduled for 06:00/06:30; both skipped entirely on the 20th). Nightly: all eight jobs green, every graded job 100% passed, none eligible yet (feature canaries 15/20 nights, the money jobs on their first). Loop: triage read the report card and answered "attention no" (no failures, no degraded grade), implement and inspect skipped, nothing opened, 22 s. The pipeline runs unattended and declines to act when there is nothing to act on. M0-2: 1 of 10 green nightlies at --retry=0. Open: the cron lands after lunch in Madrid; moving it to 02:00 UTC would absorb the delay. On `claude/stream-flow-grounding`: the streamed path let a number through at the end of a flowing sentence (196,94 EUR, a sum never computed); fixed with a test.
- **2026-09-21 (Claude, late afternoon):** #474 merged (the trace panel speaks the reader's language; every coming diary event reaches Plan; By day of the week on Month). On `claude/chat-learns-facts`: what the person tells the chat becomes the fact it is (money coming in, a subscription taken on, a charge cancelled) as a tappable offer computed by `statements.js`, with an ask-back for a missing day or a foreign currency; a cancelled series leaves the recurring table and the forecast; a one-off stated income lands in its month only; the amounts the person typed count as known to the grounding gate. Stefano asked for the Account section: it is at the bottom of You, live since #469.
- **2026-09-21 (Claude, evening):** #475 merged (what the person says becomes the fact it is). Page review from a signed-in walk of production (Today 383 words, Month 689, Plan 176, You 729 against the register's ~150): eleven findings given to Stefano; the five he took first on `claude/page-cuts`: Month's readings contradicted Today (a stale "Facebook leaves today" and "more than the 0,00 EUR left of your month" on a balance basis; dated nudges now expire off the page and the charge-ahead line stays silent on a balance basis; Month shows only the readings Today does not), the usage block with no data is gone, You keeps four of seven pattern kinds, Today's thirty-day strip is one line about tomorrow, the orb caption is gone until a ring is tapped. Left for him: the phone pitch covering every page for a new person, "Unknown" as a merchant on the pages, mixed language on You, the diary learning one kind of day twice, the band's 0 of 2, the twin link in the footer.
- **2026-09-21 (Claude, night):** #476 merged and walked on production: Today 383 to 359 words, Month 689 to 613, You 729 to 660; the usage block, the orb caption, three filler patterns and the thirty-day strip are gone; "Facebook leaves today" expired. The charge-ahead line stayed one more day because a stored nudge only expired by date: `claude/withdrawn-nudges` withdraws a nudge the refresh no longer produces (row kept for the tally, off the page at once).
- **2026-09-21 (Claude, late night):** the rest of the review on `claude/review-rest-and-orbs`: the first-run steps show on Today only (they covered Month, Plan and You for a new person); a line without a name is "A payment without a name" on the pages; You says a stored choice (family, friend, save) in the reader's language; a kind of day is keyed on its words, so a rescheduled series is not learned twice; the band's record waits for seven scored days. Kept: the footer link to the twin, pinned by `money-first.goal.test.js` (one link away, never the door). Orbs: one shape per kind of work (`orbFor.ts`): a page on its way breathes, learning shapes, a bank read is the globe, the trace weaves, the model's reasoning is the cube, writing the ribbon. The reasoning is folded: one quiet line counting the seconds while it thinks, the thought under How it got there afterwards.
- **2026-09-21 (Claude, night, review):** #477 and #478 merged. Playwright review of production after them (report as a Claude artifact): no console errors, no sideways scroll at 1440 or 402; the chat shows the globe while reading, then the cube with "Pensando há N s"; Plan loads in 5 s with the diary marks; Today 347 words, Month 583, Plan 114 (phone), You 559. One bug found and fixed on `claude/plan-no-name`: Plan's opened day said unknown. Open: Today's word count, diary names only inside an opened day.
- **2026-09-21 (Claude, late):** #480 merged. Stefano: divide the information, keep the account off You, more air. On `claude/account-page-and-air`: a fifth page, `/money/account` (Sources and the account section moved there; You is now what it worked out and what it knows), its nav link set apart under Ask; sections 160 px apart (112 on a phone), rows 88 tall with 24 px padding, the list rule 40 px under the grey line, the column 56 px from the top; CLAUDE.md and AGENTS.md updated.
- **2026-09-21 (Claude, night, after #481):** walked on production: the Account page holds the sources and the account section, its link sits apart under Ask, sections measure 160 px (112 on the phone); You is 278 words on desktop (was 559), Month 522. Seen: on a fresh browser the first paint of any money page waits 3.8 to 4.4 s on `GET /api/auth/verify` before the page read starts (You paints at 6.2 s); one outlier of 26 s. The diary still lists "alvaro psicologo" twice until the nightly relearns with the new key. On `claude/one-orb-on-you`: one orb on You while it loads (the patterns section waited with a second one).
- **2026-09-21 (Claude, night, critique):** #483 (SPA rewrites), #484 (the days ahead on Plan) merged. A critical Playwright walk of every page at both widths found and cut, on `claude/critique-cuts`: the day's figure read as money to spend while meaning the opposite (over budget now shows a minus); the band's right label carried three figures; "Tomorrow: usually about" duplicated Plan; carved tiles decorated rows that already said what they were; the review asked "was it worth it?" about payments with no name; the chat repeated "How it got there" under all eleven turns and named the speaker on every one; a two-bar history chart stood beside the same two numbers as rows; six recurring rows each opened "Recurring, every month". Measured: Today's first paint is 8 s on a cold browser, of which `auth/verify` fires at 6 s (client-side). Month is 5.8 screens. Flagged, not cut: the orbit on Month (Stefano chose it on 2026-09-15).
- **2026-09-21 (Claude, late night):** #485 (the critique cuts) and #486 (the favicon was the 1,28 MB hero photograph, fetched on every page) merged. Three prototypes published for Stefano; he took the calendar and the sources, and asked for visual and clickable rather than prose. Shipping the part that was blocked: the diary kept only the next sixty events and a slim past, so a day with class could not be compared to a free day. `dayCounts()` writes one integer a day, merged across runs (a read sees ninety days; the record outlives it), `monthPlan` carries it onto every square, Plan draws a dot an event on past days too, and `classSplit()` says what each kind of day costs once four of each are in. Next from the prototypes: the term strip of nine weeks, sources with counts and what they cannot see, and saying when a time of day is the bank's booking hour (85 of 111 September payments).
- **2026-09-21 (Claude, night):** #487 (the diary keeps its past; Plan draws a dot an event and says what a class day costs) and #488 (a part of a day says how many payments know their hour) merged. On `claude/source-counts`: the Account page says what each source has actually given, from `sourceCounts()` in seen.js, a new part of the one page read. Stefano's figures tonight: 216 sightings from the bank, 52 from receipts, 1 from a statement, 0 from the phone; this month 111 payments, 157 of 161 named by the bank, 26 carrying the hour they happened.
- **2026-09-22 (Claude):** #489 merged (the Account page says what each source gave). The term strip lands on Plan: `termWeeks()` in calendar.js counts the diary week by week, five back and three ahead, from the day counts kept since 2026-09-21. A week outside what has been read draws as a gap, never a zero; three weeks ahead and not four, because a read reaches a month out and a fourth week would always be a gap.
- **2026-09-22 (Claude):** #490 merged the term strip and it drew flat: a percentage height on the bar could not resolve, because the bar sits in a grid row its own content sizes, so every week fell back to the 3px minimum. Bars are in pixels now (BAR_MAX 104), 62 per cent of the column wide, and the strip was looked at rendered before the fix went out.
- **2026-09-22 (Claude):** #491 merged (the flat bars). At 402px the nine week labels ran into each other ("14 sept21 sept28 sept"), so the phone gets the day of the month alone and the line under the strip names the month when a week is tapped. Both widths looked at rendered.
- **2026-09-22 (Claude):** asked what the diary is actually for before building on it. `scripts/money/evaluate-class-days.mjs` reads the calendar live, writes nothing, and backtests the day's figure with and without the diary: splitting loses at 60 and at 90 days (D7). So the covariate is not shipping, and the Plan section that averaged the two kinds of day was rewritten to the middle day of each, silent unless they are genuinely far apart.
- **2026-09-22 (Claude):** #495 (the report card counts the streak; M0-2 is 1 of 10). The chat can now read the term: `calendarLines` carries the diary week by week, three weeks named (last, this, next) and the rest by date, with the weeks that have not been read left out and said to be left out. Three scenarios added (`next-week-busy`, `quiet-week`, `busiest-week`); they cannot be run live against Stefano's ledger until the morning's calendar read writes the day counts.
- **2026-09-22 (Claude):** #496 (the chat reads the term). Then the third of the three: the twin bridge is closed while the twin is parked (D8), the evaluation runner cleans up after itself, and 517 memories from my own eval runs are waiting on Stefano's word to be removed (`scripts/money/clean-eval-memories.mjs`, count-only until `--apply`).
- **2026-09-22 (Claude):** #497 merged and the 517 evaluation memories removed from Stefano's twin stream (517 rows, backed up to JSON first). The 02:26 calendar read wrote 114 day counts, so the term strip draws on real data: 6, 5, 14, 15, 19, 18 this week, 17, 13, 10. Then his own complaint about the diary's figures turned into D9, the largest correctness fix of the week.
- **2026-09-22 (Claude):** #499 (build only main) and #500 (Plan sees the diary) merged. Then the bigger half of the build bill: `googleapis` removed entirely (D12), 31,4 MB and 889 files out of what Vercel traces on every deploy.
- **2026-09-22 (Claude):** #502 restored deploying (D13) and the build measured again: 19,3 min against 21,7, the bundling gap 920 s against 1 050. The preview skip is the real saving and it works — a branch push is now CANCELED at 0,1 min. The term strip is live on Stefano's own diary (6, 5, 14, 15, 19, 18 this week, 17, 13, 10) and 22 of 30 squares carry an event count at last. Then the walk's own finding: "The days ahead" listed twenty-two unpriced classes in registrar capitals, so a day in the diary that costs nothing is off that list, and a course reaches a row as a person says it (`eventName.ts`).
- **2026-09-22 (Claude):** the three term scenarios met the real model for the first time (the diary only got day counts at 02:26). Two findings, both real: asked which week had the most classes it answered "17, the highest in the weeks read" on a term holding 19 and 18 — so the busiest and quietest weeks are computed in `calendarLines` now, with the model told not to rank them itself; and `next-week-busy` failed on an avoid rule that caught an honest caveat ("the ledger can't tell what those events cost") rather than a refusal. 2 of 3 before, both checks corrected.
- **2026-09-22 (Claude):** build-time work closed with D14 — three hypotheses measured and all three dead; the only real saving is the preview skip. Jev (typesafe.ai) reached through the OpenRouter key the project already holds (`~typesafe/jev-latest`, `POST /api/alpha/decisions`): on Stefano's 73 merchants it is 1,22 cents a thousand at a 298 ms median, and at confidence 1.0 it found 12 people among the 25 the place provider cannot place, its single disagreement being a merchant the ledger itself has wrong. Not wired in; the decision is Stefano's.
- **2026-09-22 (Claude):** Jev wired in where the measurement said it belongs (D15) and nowhere else. Two experiments, one of them a dead end, both written down.
- **2026-09-22 (Claude):** the judged categories moved per person (D16) before they ever touched another ledger.
- **2026-09-22 (Claude):** build-time work closed for good (D17). Five experiments, all negative; the enhanced build machine is not worth buying and the evidence is a support ticket rather than a code change.
- **2026-09-22 (Claude):** the 22 September audit published (grade C+; https://claude.ai/code/artifact/f2a00153-e8b5-4b53-b80c-4007aa722bd2) and the critic loop run on Today, four rounds, stopped at 6 (D18). The grounded changes shipped; the register-level asks are listed for Stefano.
- **2026-09-22 (Claude):** every open question decided (D19); four became code in one PR, the rest are answers in the block above.
- **2026-09-22 (Claude):** the Enable Banking follow-up sent on the original thread (message 1a0ca4b7f299b1b5); `LOOP_PUSH_TOKEN` set and wired into the loop. Starting the audit's plan towards A+: M0-A reach list, then M1.
- **2026-09-22 (Claude):** M1-B: every write that stays mounted runs `validate()` — 32 routes across 14 files, `stayingSchemas.js`, types and lengths only, webhooks exempt by name. Presence included (types and lengths, its words untouched) for Codex to review in the PR.
- **2026-09-22 (Claude):** the audit's nine quick wins in one PR: README for the product that exists (no `--legacy-peer-deps`); the ten parked crons out of the schedule (18 -> 8; the gate stays); `authLimiter` on GET /magic-link/verify; the privacy policy names TypeSafe's Jev and what it receives; `src/pages/MoneyPage.tsx` (995 lines, never routed) and its opt-in spec deleted; the three orphan entry points and twenty Lovable-era scripts deleted; `CLAUDE.md` stops hand-counting exports and handlers. Not done: the four moderate advisories need react-router 7 and a downgrade of `bull` to 1.1.3, so they are M2-B's (bull is twin-only) and the router's own migration.
- **2026-09-22 (Claude):** M0-A and M1-A in one PR (D20): the reach list is computed, the retired twin answers 410 at the API and one quiet line in the app. Next: M1-B, validate every write that stays mounted.
- **2026-09-22 (Claude):** M1-C: `script-src` is `'self'` and PostHog, no inline scripts anywhere the site serves. The CSP walk found `SunContext` posting GPS to a route about to answer 410 and calling a host the header already blocked; the sun, the day/night photos and the background-mode plumbing are gone. Next: M1-D, name the 76 swallowed errors in the staying set.
- **2026-09-22 (Claude):** M1-D: the 75 swallowed errors in everything the product reaches are named and counted (`quietly` moved to `api/services/quietly.js`); the goal test now walks the reach set instead of the money folder. Next: M2-A, one facts read per page.
- **2026-09-22 (Claude):** M2-A: the page and the chat read the ledger and the facts once and hand them on; 37 Supabase calls per Today became 25, seven ledger walks became one. Wall time did not move locally (parallel parts); the production canary after deploy says whether it moves there. Next: the client's first paint before `auth/verify`, then M1-E and M2-B.
- **2026-09-22 (Claude):** M2-A, client half: Today mounts from its kept snapshot before `auth/verify` answers (a known person, money paths only); money reads wait for the token's arrival. Tests: `accessTokenReady`, `protectedRouteEarlyPaint`, `moneyReadWaitsForToken`. Measurement: the canary's warm/cold paint after the deploy.
- **2026-09-22 (Claude):** M2-B, the front half: 308 of 540 files under `src/` were reached by nothing once the twin's routes left `App.tsx`; deleted, with `scripts/ci/front-reach.mjs` and `tests/goals/front-reach.goal.test.js` keeping the count at zero. Type errors 109 -> 31, lint 66 -> 46, chunks 97 -> 60. Next: M2-C, the 31 type errors that are left and strict checks; the API half of M2-B waits for its 30 days of 410s (D20).
- **2026-09-22 (Claude):** M2-C: strict TypeScript on for `src/` and zero errors; zero lint errors across the repository (`api/mcp-server` and 16 stale root specs deleted; every other error fixed at its cause). Baselines read 0 and 0. Next: M2-D, the 112 routes that read the database directly; the API half of M2-B on 2026-10-22.
- **2026-09-22 (Claude):** D21: preview builds off for every branch (thirteen in an hour today, about nineteen minutes each); `main` and `bisect/*` still build. M0-C: a coverage floor over the whole staying set (36% lines), beside the money core's 65%.
- **2026-09-22 (Claude):** M2-D: every table call in the 23 staying route files is a named store function (125 calls, eleven stores); `routesWithDirectFrom` 112 -> 93, the rest parked. Production walk after the header change: zero CSP violations on sign-in.
- **2026-09-22 (Claude):** M0-B recorded as met by the `Money browser` job. Correction: #522 overwrote the M2-3 goal test (`money-one-read`: Today is one request, no third-party stylesheet) with the new server-side one-read test; the old test is restored unchanged as `money-one-request.goal.test.js`.
- **2026-09-23 (Claude):** M2-A client half measured on a production React build against a local API, as the canary measures it: warm Today paints in 37-46 ms from the kept snapshot with the page read leaving at 23 ms beside `auth/verify`, not after it; cold (no cached user) 1,9 s locally, of which the verify round trip is 650 ms. Found on that walk: with the bank itself short (157,31 EUR reported, 330,13 EUR of phone-seen payments not booked) the hero said "172,82 EUR past the 172,82 EUR in your bank"; it now says what the bank has and what is still to book, on the server and the page, in three languages. Today measured 198 words against the 180 budget on this ledger (three upcoming items, three readings); the nightly canary will say so, and the words are a design call for the owner.
- **2026-09-23 (Claude):** #527 merged and live. The nightly dispatched against main: Today painted in 5,4 s cold and 151 ms warm (yesterday 9,5 s and 473 ms; the early paint is the difference), one page read; red on two counts, both the canary's own: it looked for the sources on You since they moved to Account (#528), and it counted the phone sheet in Today's words (D22). Two of ten green nightlies at `--retry=0` stand; the retry stays.
- **2026-09-23 (Claude):** walked all six money pages on production as the owner, from a short-lived token, and fixed what read wrong: every load still called the parked `/api/onboarding/new-user-check` (410) from AuthContext, gone; Sentry's ingest host was not in `connect-src`, so production had reported nothing since the header shipped, allowed now; an arrival nobody mentioned read "Comes in, as you said" on Today while Plan called it "seen before, not said" (the forecast dropped `times`, the page defaulted to "said"); a payment the bank did not name was listed as a shop called "unknown" on Today and Month; the Account's sources line put all-time sighting counts under a thirty-day payment count; "and book tomorrow" was ungrammatical; "3.3 times" sat on a page of commas. Seen and left for the owner: the Month hero says 1623,18 EUR spent while its month-pace reading, computed at night, says 1621,48 EUR; the Lodging category counts a 100 EUR transfer to Mauad G. as spending while Plan expects 100 EUR from Mauad G. to come in; Ask refused to total yesterday's four payments.
- **2026-09-23 (Claude):** Mauad G. (the owner: "100 euros he sends me, and it depends a lot"). Four things were wrong at once: the place lookup asked Google about "Mauad G." and got Mauad Hotel Santo Domingo, 6,000 km away, so the merchant carried kind hotel and category lodging; a 100 EUR phone sighting under that name, already marked not_me, still stood in Month's categories because `categorySpend` never read the verdict; the Bizum short form "mauad g" did not resolve to the person fact "mauad gebara christian"; and the unsaid, 50%-on-time arrival set today's horizon to one day. Now: a name with an initial is never looked up; a hit outside the country is refused; rejected rows leave the categories; a short form resolves to the person (`roleOf`); an unsaid arrival under 0.75 confidence stays on the page but does not drive the day. The hotel row is deleted (`scripts/money/forget-place.mjs`).
- **2026-09-23 (Claude):** the owner, on Mauad G.: "he's my father and I put that already; the system should know." It did not: "Family, on the 1st, 1750" named no sender, and the father's series was matched by neither name nor amount, so his 1750s believed the stated income at half while his 100s became an unsaid arrival on the 24th. `incomeEvents` now matches a stated income to its sender by the person's role (the fact says family; the sender was named family), dates it from the arrivals of the same kind of money (within half to double the stated amount), and never turns a named person's other transfers into an arrival to count on. An unnamed sender needs to land near its day 60% of the time before it is one either.
- **2026-09-23 (Claude):** Ask, depth: asked for software from the most expensive down, the chat had said the kind's total and its largest and stopped, because the rule told it the shares figure was the table. Two computed lines join the context (each kind's payments this month largest first, names capped at eight; each kind by month for the last four) and the rules read them: a table is every name and amount in order then the total, "every month" is each month's figure and never an average. The grounding receipt (`basisOf`) covers every number in the words before it fills its eight lines; a seven-row table used to leave its own line out. Eval against the real ledger, two runs each: software-table 0/2 -> 2/2, software-every-month 1/2 -> 2/2, where-ask and yesterday-total 2/2. Full set once: see the PR.
- **2026-09-23 (Claude):** Stefano: "i dont want u to hardcode data for my acc, it should be specific for each user and each account". Audit: no code path keys on his account; seven places wrote Spain as the country and one the Spanish language for lookups, and currency and timezone are one value per deployment. M3-7 opened: stage one threads country and language from the person's own data; stage two (timezone, currency) dated. On connecting banks: PSD2 bank APIs admit only licensed AISPs (six to nine months, eIDAS certificates, PI insurance); Enable Banking is that licence used on our behalf. What blocks other people today is the production app in restricted mode (only the owner's linked accounts come back) pending contract and KYB, and the allowlist gate in `betaCapabilities.js`. GoCardless Bank Account Data is closed to new signups.
- **2026-09-23 (Claude):** Ask, depth, second round on the eight scenarios that failed stably after #534. A short follow-up ("no, I meant last month") now carries the previous message into the computed lines; when the model's every sentence is dropped for a figure the ledger does not hold (which is what adding the parts up produces), the ledger names the parts itself, each kind with its own figure in the month asked, in the page's words for the kinds (es/pt). The whole name a person types finds the shorter key the bank gave ("Maria Dolores Tomas Obon" -> "maria dolores tomas"), so the landlord offer is no longer dropped. Money that comes "sometimes" asks for no day and offers no dated income; the words are kept instead. Coffee reads as eating out; a stretch whose line shows nothing is "nothing was spent", never "cannot tell". Scenario fixes: weekend-before's history carried a real date that has since become the weekend before last; rent-due and coffee-week say the true thing and the judge is off for them. `MONEY_CHAT_DEBUG=1` logs what the model proposed before the check, for the eval loop.
- **2026-09-23 (Claude):** Ask, third round: two replies need no model (the one missing thing of a statement is asked in the ledger's words; a message that instructs the assistant keeps nothing), and who somebody is becomes the person offer by computation (a role word and a name that finds a transfer or Bizum counterpart, three languages). Full set before this round 88/94 (83 in the morning); the eight stable failures of the morning all pass, landlord 3/3. `MONEY_CHAT_DEBUG=1` showed most model replies arrive as prose, where no action can exist, which is why offers must be computed.
- **2026-09-23 (Claude):** Month: the pace reading ("By the 23rd you had spent ...") said the numbers stored at the last refresh while the hero above it said this load's, and a morning's payments sat between them. The page now hands the sayers the hero's live figures (`LiveFigures`: the month so far, the same days of last month, today's day) and the pace reading is re-said from them when it is about the month on screen; the stored line stands for any other month or a missing figure. Design call two of three (the owner, 2026-09-23).
- **2026-09-23 (Claude):** M3-7 stage two, the timezone. The zone helpers read the request's zone when given none (AsyncLocalStorage), set once per request, per cron person and per WhatsApp message from the cached profile, so nothing in the pipeline changed signature and everything moved to the person's zone. The page reads its days in the payload's zone: three UTC reads (today's day, the orbit marks, a late income's last day) were a day behind in Madrid between midnight and two. #537 and #538 merged; full chat set 88/94 before the third round.
- **2026-09-23 (Claude):** M3-7 stage three, the currency, on the same scope as the zone. Twelve euro reads and two euro-only refusals now follow the person's own currency; the page's formatter and six filters follow the payload. Conversion stays absent by design. #537 live in production: the Month hero and its pace reading say the same figure. M3-7 closed.
- **2026-09-23 (Claude):** M3-8: the bank gate computed per person (linked, listed, or unrestricted and served), the reason said on Sources and in the 403s; phone capture open to everyone (D23). Stage three of M3-7 (#540) merged; its build is next to go live.
- **2026-09-23 (Claude):** bank access decided (D24) after reading the providers and the mailbox: quotes from Enable Banking (form) and Arcopay in parallel, the rest rejected with reasons; the decision page holds the drafts. #541 (bank gate per person) in CI.
- **2026-09-23 (Claude):** Ask, fourth round, on the day's closing 89/94 (figures, actions, length, style, grounded all 94/94). Three answers need no model now: a statement that produced its offer (income, subscription, cancellation, who somebody is) is answered with the offer's own sentence and the card, in three languages, under a second; one stretch whose line shows nothing is "Nothing was spent" with the hours caveat; "this weekend" asked midweek gets a line saying it has not come, so the model stops re-dating the weekend before. A real bug: "on the first of every month" was not read as day one and the ledger asked for the day it had just been told; the day is read as a word now in three languages, and "me mandan" joins the income verbs. Twelve scenarios, two runs each: 22/24 before the last fix, the two misses the judge on a true computed answer (judge off there).
- **2026-09-23 (Claude, afternoon):** with the owner's authorisation: the Arcopay mail sent from his Gmail (Basic plan price, what a connection is, time to production, agent status); Enable Banking's control panel read in his Chrome after a fresh sign-in link: the production application is active, "Account Information Restricted", one linked Santander account, privacy and terms links valid (twinme.me/privacy-policy and /terms both answer 200), data-protection email his Gmail. "Request unrestriction" opens the same quote form as Billing: sections 1-4 (AIS, countries, "not authorised, needs to contract", use case, effective date) plus company name, country of incorporation, registration number, and the contact's title and phone; the panel says unrestricted production access is for organisations only. A test to privacy@twinme.me from his Gmail did not come back to his inbox in ten minutes (Gmail hides a self-addressed copy); a second test from the app's Resend sender landed in his inbox within a minute: privacy@twinme.me forwards. The quote form is filled in his Chrome (AIS, 10 accounts in month one, 100 in month twelve, 500 in month twenty-four; Spain; partner setup; the use case; effective 1 October 2026; the one registered business his mailbox holds, a Brazilian MEI with its CNPJ; contact Stefano Gebara, Founder); the harness refused to submit a commercial form on his behalf, so the phone, the confirmation box and Submit are his.
- **2026-09-23 (Claude):** walked production as an invited student with no bank (the stranger account, a Santander-style statement of six weeks, desktop and phone, Spanish): the onboarding skips the bank step and offers places and the phone; Sources says why the bank is closed; the import read 46 rows and added 46; Today, Month and Ask answered with no console errors and no failed requests. Four things were wrong and are fixed: a bare concept (no COMPRA) was read as a transfer, so a cinema and a metro fare sat under transfers and "METRO DE MADRID" became money from somebody called Madrid (now a card payment when money goes out, a transfer when it comes in, in the importer and the bank feed alike); the band's strip said "Probablemente 394,03 € para el 30" under "too early to say" (now "No forecast yet"); a reading said "transfers" inside Spanish (the kind has its word now); the phone's Account counted the bank and receipts but not the statement's rows (statements are counted). Re-walk after the build.
- **2026-09-23 (Claude):** Ask, the shape of a kind answer. Stefano asked "How's software expenditure?" and got one paragraph with this month's list, four months by comma and six recurring charges of other kinds, under a figure of the whole month's kinds. One kind and one month asked how much is a computed shape now: the total and count, the largest, how last month closed, what comes back in that kind only, and when the bank's lines were last read; under it the table of that kind by place, largest first, with the bank rows as receipts; in the language written. Comparisons, "why", stretches and "every month" stay with the model, whose own kind answers now draw that kind by place. Six new scenarios (en, pt, es, groceries, why, a named month) pass twice each in under a second; the harness checks the figure's kind.
- **2026-09-23 (Claude):** re-walk after #545: Month files the metro under transport, the cinema under entertainment, Zara under clothing, the Bizum alone under transfers; the phone's Account counts the statement's 46 rows; production computes `restricted` for the stranger. Found on the way: the language step's choice reached the account but not the app's cached user, which only refreshes with the token, so the sheet asked again on the next full load and the pages stayed in English until a refresh happened to land (the first walk's long import had renewed the token, which hid it). The auth context now patches the cached user the moment a language is kept, on the sheet and on the Account row. Ask on production answered the week question in Spanish in 10,7 s cold.
- **2026-09-23 (Claude, evening):** both bank quotes in. Enable Banking EUR 500/month discounted (EUR 1/account, min EUR 1,500 standard), and five linked accounts free for evaluation; Nuek/Arcopay wants a call. D25: pilot five friends free now. The Gmail connector's session expired after the account switch; the mails were read in Stefano's Chrome.
- **2026-09-23 (Claude, night):** M3-9, the Ask benchmark, on the owner's ask for "the most comprehensive audit": 109 turns in ten dimensions with a grader, run six times through the day, 77 -> 104 of 109, the last five misses model latency (one at 47 s) and a computed reply at 3,0 s. Twelve defects fixed on the way (the tracker row lists them; two were production bugs nobody had reported: recategorise 500'd, every forecast read waited 27 s on scoring). Two lessons for the harness: run it under `caffeinate` (the Mac slept ten minutes mid-run and one question logged 636 s), and the run spends the person's per-user LLM budget, which downgrades to the same model. What the benchmark could not fix: DeepSeek's latency tail, one turn in fifteen above 20 s. Report: https://claude.ai/artifact/Vaqj1c7aSSn1K4mg8y5SGh (the Ask Benchmark artifact).
- **2026-09-23 (Claude, late):** #548 merged (the benchmark). Stefano brought four ideas (bank alert mails from Gmail, statements by mail, a recurring watch, a live sheet in Drive) and the Instinct walkthrough video; both read and mapped in `.claude/plans/2026-09-23-instinct-walkthrough/README.md`. First one built as M3-10 (#549): Gmail's forwarding confirmation caught and shown on Sources, so a filter from the bank to the ledger address is the whole setup. The weekly WhatsApp summary stays behind DISCUTIR 10/15.
- **2026-09-24 (Claude):** #552 merged and live: the production build of the move took **63 s** end to end (Vite 13 s, function 30 s, deploy 10 s) against 19 minutes; health and the Today read answer through the one function. Behind it #551, #553 (the sheet), #554 (reactions), and this: the account remove (M3-14).
- **2026-09-24 (Claude, night):** #552 (one function) waiting on CI after a coverage-floor fix: on CI the glob under `api/_app` matched nothing, so the floor measured the pages alone; the config names the API files now. Stacked behind it: #551 (inbox on the page), the month as a sheet (M3-12), reactions as answers on WhatsApp (M3-13). Stefano: forget the legacy platforms, money from scratch, the WhatsApp agent modelled on Instinct.
- **2026-09-24 (Claude):** the parked API deleted (M2-B api), thirty days early on the directive: 331 files, 158 mounts, 117 tests; the function that Vercel builds holds the money product and what serves it. Also today: the suggestions (#556) and the flake fix (#557) merged; the sheet (#553) and the voice notes (#558) in the queue.
- **2026-09-24 (Claude):** production went down a second time, 12:33 to the fix below, on the parked API's deletion: seven dynamic imports of deleted routers survived a deletion script that only knew static ones, and `app.use('/api/costs', (await import('./routes/cost-dashboard.js')).default)` runs at the top level of server.js. The load guard caught it the minute it existed. Two guards now: `entry-loads.goal.test.js` boots `api/index.js` in a real Node process, and `no-dead-imports.goal.test.js` resolves every specifier in the API against the disk, dynamic included. Both were proved by breaking the tree on purpose and watching them fail.
- **2026-09-25 (Claude), the sheets:** two pieces of the same idea, that a spreadsheet is a source and not just a file. M4-7 reads a sheet nobody designed for us, with a model reading the shape and never the figures and the questions computed from the rows; M4-8 re-reads a living one on a schedule. The second was cheap only because of the first and because `source_ref` has been content-addressed since the statement importer was written: **the dedupe that was built to stop a re-uploaded file doubling a month is exactly the dedupe a sync needs**, so there is no diffing and no cursor anywhere in it.
- **2026-09-25 (Claude), the ocean:** asked to decide whether to attack the cold start, and the answer was that there was barely a cold start to attack: 629 ms cold against 307 ms warm on a route that touches nothing. The ten seconds was geography. The function had no region set, so it ran in Washington DC while the database is in Paris and the people are in Spain, and one database round trip cost 650-1 600 ms. One line of `vercel.json` later it costs 140-200 ms and `/auth/verify` went from 4 563 ms to 218 ms (M4-5, #579). **The lesson is to measure the layer before choosing the fix:** the plan an hour earlier had been to trim Sentry and lazy-mount routers, which would have bought a fraction of a second of the wrong thing.
- **2026-09-25 (Claude):** the sign-out on every reload was three tabs racing one cookie jar through a rotating refresh token (M4-3, #576). Then Today's paint, measured twice: first badly, with a polling loop that forced layout and inflated its own reading, and then properly with the canary's method (M4-4). Warm is 141 ms and cold is 10,5 s, so the page is not slow, the cold start is. **The lesson is that a measurement that touches the page is part of what it measures**; `measure-prod.mjs` and `race-check.mjs` are both in `scripts/money/tmp` so neither has to be rebuilt from memory.
- **2026-09-24 (Claude), the walk:** signed in as the owner on production (magic link to his own inbox) and read the money pages against the database rather than against a screenshot. The repair had held: duplicate-looking pairs 12 -> 0, the 500,00 EUR double arrival gone, and a nameless 1,85 EUR alert from that evening was joined and named by the bank's pending row at 20:17 while the page was open, which is the new reconcile rule working live. `is_subscription` flipped on the page read itself: five subscriptions, Facebook's biweekly ad spend correctly out, and the chat began offering "Which subscription is the least worth it?", a question that had been unreachable for every person since the flag was written. The walk then found three more faults, none of which a test was watching (M4-2, #571). **The lesson is the method:** the screenshot said the ledger was stale and it was not; reading the pages against the bank is what found the real faults, and `scripts/money/audit-ledger.mjs` now does that part on demand.
- **2026-09-24 (Claude):** audited the production ledger against its bank evidence after Stefano showed the week's review calling 30,00 EUR what weighed most. The ledger was not stale: the bank pull was 0,0 days old and payments existed on every day to the 24th. It was polluted. Ten lines opened by Santander's own alert mails stood beside the rows they announced, and one of them doubled today's 500,00 EUR arrival. Fixed at the reconcile rule, not the page (M4-1, #569). Two further faults fell out of the same audit: `is_subscription` has been false for every series of every person since the flag was written, because it reads a table that has never held a row; and the review's line describes a ranking while showing a queue. Stefano ran the repair: **7 lines folded**, the 500,00 EUR double arrival gone, duplicate-looking pairs 12 -> 0, nameless lines 10 -> 4. #569 merged and deployed (7246168c), so no new phantom is opened. The three the first pass kept are a second alert against a figure the bank booked once; #570 adds `--settled`, which folds an alert the four-day window has outlived (Bolt 19,00 and Playtomic 22,36) and still leaves alone anything the bank may yet book. `is_subscription` flips on the next page read or cron: the row is recomputed and upserted every time.
- **2026-09-24 (Claude):** the Ask benchmark after the day's merges: **108/109**, against 99 before the ledger read was parallelised (M3-17). Every speed failure cleared (speed 109/109, grounding's median 3,712 ms -> 1,052 ms). Its one failure was real and wider than the scenario: asked for another language, the chat refused in that language, because the model was re-reading its own stale sentence. M3-18 fixes it by re-answering the question from the ledger.
- **2026-09-24 (Claude):** production was down from 00:10 to 12:24 (FUNCTION_INVOCATION_FAILED): `sheet.js` (#553) imported a name `store.js` did not re-export, and an ES module's missing export is a link error, so the one function could not load. Nothing caught it: every unit test mocks the store, and vitest's transform turns a missing named export into `undefined`. The first hotfix (#561) shipped only its test, because a `git checkout -- .` before staging reverted the edited file; #562 carried the line and production came back a minute after it merged. The guard is now `tests/goals/entry-loads.goal.test.js`, which loads `api/index.js` and `routes/money.js` in a real Node process and fails on the broken tree with production's own message. Lesson in `tasks/lessons.md`.

- **2026-09-25 (Codex):** #575 merged at 63b75a12e; production verification in progress. Claimed next quality slice on `codex/money-quality-next`: mask card identifiers at the evidence presentation boundary (API + behavior tests), libraries.dev prompt/variation review, and an Ask composition iteration. Original source records remain intact. Backend privacy work and design research run independently; no production migration is part of this slice.

- **2026-09-25 (Codex, quality follow-through):** Extend the evidence slice to propagate database failures, distinguish failed/empty/loading evidence on web and native with retry, and mask explicitly card-labelled identifiers in XLSX merchant fields. These are presentation/read reliability changes, with no source-record mutation.

- **2026-09-25 (Codex):** libraries.dev comparison found a real accessibility gap: a mounted orb ignored a changed reduced-motion preference. Claimed and reproduced with a failing lifecycle test; update the wrapper listener and cleanup while retaining the existing library geometry and monochrome output.

- **2026-09-25 (Codex, doing):** Native lifecycle review confirmed kept-mounted Month/Ledger/Plan do not re-read on tab return or foreground, and Month ignores completed bank pulls with zero inserts. Claimed a coalesced visible-screen refresh hook, active props from Shell, Chat history refresh on tab return, and regression checks; no new polling or bank reads on navigation.

- **2026-09-25 (Codex, ready for CI):** Evidence masking/error recovery, native active-view refresh, corrected sign-in copy, and live reduced-motion support are separated from the uncommitted Ask design study. Latest validation: 4,520 root tests passed (11 skipped), 19 native handler/lifecycle tests, 60 desktop/phone browser checks, web and mobile types. Independent code review found two Medium issues (rejected refresh queue and malformed web evidence); both fixed with regressions and re-reviewed. Native simulator runtime/Android GUI verification remains partial; tests do not substitute for it. Libraries.dev: all seven public prompts copied, 19 public variants inspected; no new package installed. Design review remains below 9/10 and continues outside this reliability release.
