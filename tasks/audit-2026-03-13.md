# TwinMe Platform Audit — 2026-03-13

5 parallel agents: Backend Health, Frontend E2E, UI/UX Review, Security Scan, Data Integrity

## CRITICAL (4)
1. Stack trace leaked in CORS error response — api/server.js:~156
2. RLS disabled on user_finetuned_models table
3. Hardcoded JWTs in test files committed to git (5 files)
4. Nango webhook signature verification bypass — api/routes/nango.js:796

## HIGH (9)
5. SKIP_CRON_AUTH bypasses all cron auth (no production guard)
6. memory_links always-true RLS policy (anon can access)
7. Leaked password protection disabled in Supabase Auth
8. RLS initplan perf on 8 core tables (auth.uid() not wrapped)
9. JWT error detail leakage in 401 responses
10. Health endpoint exposes environment name
11. FK reference split: auth.users vs public.users (30+ tables)
12. Redis-dependent brute-force protection (falls back to no-op)
13. Analytics event injection (no auth, no payload size limit)

## MEDIUM (17)
14. Embedding cache hash collision risk
15. Auth redirect inconsistency (/dashboard -> / vs /settings -> /auth)
16. Dead footer links (/get-started, /soul-signature)
17. Auth page year shows 2025
18. No password complexity requirements beyond length
19. stream() lacks timeout — llmGateway.js:454
20. Duplicate token refresh systems
21. Row-by-row DB updates in forgetting cron
22. Supabase project URL in CSP header
23. Rate limiting generous on expensive endpoints
24. Functions with mutable search_path
25. Nav anchor links may navigate instead of scroll
26. Auth page missing right-panel gradient (vs Figma spec)
27. Vite/esbuild CVE (dev-only, GHSA-67mh-4wv8-2f99)
28. Weak JWT secrets in test files
29. Tier 5 homeostatic regulation fetches 5000 rows client-side
30. refreshLocks Map potential slow leak at scale

## LOW (15)
31-45. Minor improvements, safe defaults, dev-only issues.

## Summary: 4 CRITICAL | 9 HIGH | 17 MEDIUM | 15 LOW = 45 total

## Priority Fix Order
1. [x] CRITICAL #1 — Strip stack trace from CORS error
2. [x] CRITICAL #2 — Enable RLS on user_finetuned_models
3. [x] CRITICAL #3 — Remove hardcoded JWTs from test files
4. [x] CRITICAL #4 — Fix Nango webhook auth bypass
5. [x] HIGH #5 — Production guard on SKIP_CRON_AUTH
6. [x] HIGH #6 — Fix memory_links RLS policy (restricted to service_role + authenticated)
7. [x] HIGH #9 — JWT error details already gated by NODE_ENV === 'development' (verified safe)
8. [x] HIGH #10 — Removed environment name from health endpoint
9. [x] HIGH #12 — Added in-memory fallback for brute-force lockout when Redis unavailable
10. [x] HIGH #13 — Added payload size limit (10KB) + field validation to analytics endpoint
11. [x] HIGH #8 — RLS initplan optimization: 25 policies across 18 tables now use (SELECT auth.uid())
12. [ ] HIGH #7 — Leaked password protection (requires Supabase dashboard toggle)
13. [ ] HIGH #11 — FK reference split auth.users vs public.users (deferred — 30+ table migration)
14. [x] MEDIUM #17 — Auth page year updated from 2025 to 2026
15. [x] MEDIUM #19 — Added 90s stream timeout (AbortController) to llmGateway stream()
16. [x] MEDIUM #24 — Fixed mutable search_path on search_memory_stream + get_daily_memory_counts
17. [x] MEDIUM #30 — Added safety valve for refreshLocks Map (60s max lock + periodic cleanup)
18. [x] MEDIUM #14 — Replaced truncated DJB hash with FNV-1a over full text in embeddingService
19. [x] MEDIUM #18 — Added password complexity (upper + lower + digit + max 128 chars) to signup
20. [x] MEDIUM #21 — Batched row-by-row DB updates in forgetting cron (Tier 3 + Tier 4)
