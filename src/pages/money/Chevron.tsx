import { ChevronRight } from 'lucide-react';

/** The one action a row may end in: a chevron. */
export default function Chevron() { return <ChevronRight className="mv-chev" size={16} strokeWidth={1.75} aria-hidden="true" />; }
