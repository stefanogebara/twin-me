# TwinMe Platform Audit — 2026-03-15

**Audited by**: 4 parallel agents (Backend Health, Frontend E2E, Security Scan, UI/UX Design System)
**Status**: Supabase DB DOWN (Cloudflare 522) during audit — backend findings reflect degraded state
**Frontend E2E**: Re-running with real Google auth (not demo mode)

---

## CRITICAL

### SEC-1: `.env.test` committed to git with real JWT token
- **File**: `.env.test` (root)
- **Fix**: Add `.env.test` to `.gitignore`, rotate token

### SEC-2: TOGETHER_API_KEY exposed in subprocess args
- **File**: `api/services/finetuning/finetuneManager.js:59`
- **Fix**: Pass via stdin or environment variable to child process

### SEC-3: `/api/queues/health` unauthenticated
- **File**: `api/routes/queue-dashboard.js`
- **Fix**: Add auth middleware

### SEC-4: Extension tracking endpoints have NO auth (write access for any user)
- **File**: `api/routes/soul-extraction.js` lines 1570, 1637
- **Endpoints**: `/api/soul/extension-tracking`, `/api/soul/extension-historical-import`
- **Issue**: Accept arbitrary `userId` in body, write to DB without any auth
- **Fix**: Add auth middleware, use JWT userId instead of body userId

### SEC-5: CRON_SECRET not configured — all cron jobs cannot execute
- **Issue**: 10 cron endpoints all return 401, background jobs (token refresh, observation ingestion, memory maintenance) are dead
- **Fix**: Set `CRON_SECRET` in `.env`

### UI-1: CSS root color tokens use wrong values
- **File**: `src/index.css` `:root` block
- **Fix**: Update to match Figma light mode tokens

### UI-2: Inconsistent glass surface treatment
- **Files**: ChatInputArea.tsx, StandardCard.tsx, sidebar components
- **Fix**: Apply glass surface consistently

### UI-3: Hardcoded colors instead of CSS variables
- **Files**: Multiple components
- **Fix**: Replace with design system CSS variables

---

## HIGH

### SEC-6: JWT verify() missing algorithms option
- **File**: `api/middleware/auth.js`
- **Fix**: Add `{ algorithms: ['HS256'] }`

### SEC-7: `.env.test` not in `.gitignore`
- **Fix**: Add to gitignore

### SEC-8: Error messages leak internal Cloudflare HTML (infra details)
- **Issue**: When Supabase is down, endpoints like `/api/twins` return full Cloudflare HTML as error message, exposing project ID
- **Fix**: Sanitize DB error responses in error handler

### SEC-9: Gmail webhook auth conditional on env var
- **File**: `api/routes/gmail-webhook.js`
- **Fix**: Require auth unconditionally

### SEC-10: 7 tables in migration 002 missing RLS
- **Fix**: Add RLS policies via Supabase dashboard

### UI-4: Font hierarchy wrong — Geist over Inter
- **Fix**: Set Inter as primary UI font

### UI-5: Sidebar uses shadcn theme variables
- **Fix**: Replace with TwinMe design system variables

### UI-6: ChatInputArea still uses old rgba colors
- **Fix**: Apply glass surface treatment

### BE-1: No circuit breaker for DB failures
- **Fix**: Fail fast after detecting DB outage

### BE-2: Extreme response times on fallback paths (97-117s)
- **Fix**: Add global request timeout (15s max)

---

## MEDIUM

### SEC-11: Rate limiting not verified on all auth endpoints
### SEC-12: Test/demo endpoints publicly accessible (`/api/soul/demo/:platform`, `/api/soul/extract/test-youtube`)
### SEC-13: Analytics endpoints (`/api/analytics/events`, `/api/analytics/session-end`) have no auth
### UI-7: StandardCard missing glass surface
### BE-3: Empty error messages from memory-stats
### BE-4: No request timeout middleware

---

## LOW

### BE-5: Queue health shows queues not initialized (informational)
### UI-8: Minor spacing inconsistencies

---

## Frontend E2E Audit (Real Auth — completed)

Note: Real auth token returns 401 when Supabase is down (verify endpoint hits DB).
App fell back to demo mode. Findings below reflect what users see.

### CRITICAL (Frontend)

**FE-1: Auth fails completely when backend is down — infinite loading**
- AuthContext clears token + cached user on 401 from degraded backend
- Fix: Preserve cached user with degradation warning banner

**FE-2: Multiple pages show infinite loading with no timeout/error fallback**
- `/privacy-spectrum` — spinner forever, no demo data, no error state
- `/talk-to-twin` — spinner forever, no chat UI at all
- `/admin/memory-health` — header renders, then spinner forever
- `/interview` — spinner forever, no error state
- Fix: Add 10-15s timeout with error state + retry button

### HIGH (Frontend)

**FE-3: Talk to Twin (core product) shows zero UI in demo mode**
- No chat input, no conversation history, no sample messages — just spinner
- This is THE core feature

**FE-4: Dark mode CSS variables diverge from Figma**
- `--glass-surface-bg`: `rgba(255,255,255,0.06)` vs Figma `rgba(72,65,65,0.6)`
- `--glass-surface-border`: `rgba(255,255,255,0.10)` vs Figma `rgba(94,86,86,0.6)`
- `--background`: `#1C1917` vs Figma `#110f0f`

### MEDIUM (Frontend)

**FE-5: Several nav routes don't exist**
- `/insights` (only `/insights/spotify`), `/training` (actual: `/brain`), `/platform-status`, `/memory-dashboard` (actual: `/admin/memory-health`), `/help`

**FE-6: Dashboard chat input lacks glass surface styling**
- Has `background: transparent`, `border: none` instead of glass surface

### LOW (Frontend)

**FE-7: Sidebar nav border-radius 9999px vs Figma 32px**
**FE-8: Demo banner uses hardcoded rgba instead of CSS variables**
**FE-9: 8 dead footer links on /discover page**

### Page-by-Page Summary

| Page | Route | Render | Error Handling | Mobile |
|------|-------|--------|---------------|--------|
| Dashboard | /dashboard | OK | Demo data works | OK |
| Soul Signature | /identity | OK | Demo data works | OK |
| Privacy Spectrum | /privacy-spectrum | BROKEN | No fallback | N/A |
| Connect Platforms | /get-started | OK | Demo data works | OK |
| Brain | /brain | OK | Demo data works | OK |
| Memory Health | /admin/memory-health | PARTIAL | No fallback | N/A |
| Settings | /settings | OK | Demo data works | OK |
| Goals | /goals | OK | Demo data works | OK |
| Talk to Twin | /talk-to-twin | BROKEN | No fallback | N/A |
| Discover | /discover | OK | Static content | OK |
| Spotify Insights | /insights/spotify | OK | Demo data works | OK |
| Interview | /interview | BROKEN | No fallback | N/A |

