export function useBrainThemeColors() {
  const textColor = '#1F1C18';
  const textSecondary = '#8A857D';
  const textMuted = '#8A857D';
  const textFaint = '#8A857D';
  const subtleBg = 'rgba(0, 0, 0, 0.05)';
  const bgColor = '#F7F7F3';

  return { textColor, textSecondary, textMuted, textFaint, subtleBg, bgColor };
}
