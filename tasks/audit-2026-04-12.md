# TwinMe Audit — 2026-04-12

## Summary
| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH | 3 |
| MEDIUM | 10 |
| LOW | 7 |
| **Total** | **20** |

Previous audit (2026-04-03): 9 issues.

---

## HIGH

- [x] H1. **4 platforms expired tokens (GitHub, Discord, Reddit, LinkedIn)** — Reconnected via Chrome OAuth flows. All 10 platforms now active.

- [x] H2. **`/api/debug/platform-fetch` in production without dev guard** — `api/server.js:661` registers unconditionally. Fix: wrap in `if (process.env.NODE_ENV === 'development')`.

- [x] H3. **MCP HTTP server: open CORS + unauthenticated `/tools`** — Fixed: `allowedOrigins` allowlist added, `/tools` route requires `x-mcp-key` header auth.

---

## MEDIUM

- [ ] M1. **`/api/soul-signature/profile` returns `{}`** — Broken endpoint. Archetype last generated Mar 20 (3+ weeks stale). Twin chat uses twinSummary so chat quality unaffected.

- [x] M2. **`/brain` page: HTTP 410 console error** — Fixed: removed DriftAlert import and usage from BrainPage.tsx.

- [x] M3. **Owner email hardcoded** — Fixed: removed `'stefanogebara@gmail.com'` fallback from admin-beta.js. ADMIN_EMAILS env var only.

- [x] M4. **Mobile: description text severely truncated** — Fixed: `line-clamp-2` on DepartmentCard descriptions, PlatformTile descriptions, Settings feature descriptions.

- [x] M5. **Mobile: toggle/action buttons too small** — Fixed: `min-h-[44px]` wrappers on DepartmentCard toggles, ConnectedPlatformsSettings Disconnect/Reconnect buttons.

- [x] M6. **Wiki mobile overflows 47px** — Fixed: WikiGraphPage.tsx uses `flex-col md:flex-row` two-panel layout, stacks vertically on mobile.

- [ ] M7. **Personality wiki domain stuck at v1** — Organic: `compileWikiDomain` skips when no new tagged reflections since last compile (`totalNew === 0`). Not a code bug.

- [x] M8. **`cron-platform-polling.js` + `cron-personality-validation.js` not in vercel.json** — Files don't exist and are not registered anywhere. Already resolved (removed in earlier session).

- [ ] M9. **All browser extensions allowed in CORS** — `api/server.js:149-152`. JWT still required so limited blast radius, but worth locking down.

- [ ] M10. **Auth rate limiter may not cover forgot-password** — Only covers signin + signup. Verify any reset-password route is covered.

---

## LOW

- [ ] L1. Zero proactive insights — pipeline may be starved with 4 dead platforms.
- [ ] L2. YouTube connector shows `status: partial`.
- [x] L3. `llm_wiki` feature flag not appearing in flags API response — Fixed: added to USER_FLAGS + FLAG_DEFAULTS in feature-flags.js.
- [x] L4. Queue dashboard 503 fallback: leaks Redis-not-configured info without auth — Fixed: returns 404 with authenticateUser + requireProfessor guards.
- [x] L5. `SKIP_CRON_AUTH` bypass in non-production — Documented warning in .env.example.
- [ ] L6. Mobile identity: Share button 52×20px, tab buttons 38×26px.
- [ ] L7. Mobile brain: filter tabs 28px tall.

---

## What's Healthy

- Zero console errors on 10/11 routes (/brain has 1 non-critical 410)
- All 11 routes render content — no blank pages
- Zero hardcoded secrets
- All admin routes have auth guards
- 17 cron schedules all compliant (no */15 violations)
- No XSS vectors, no SQL injection, timing-safe cron auth, Helmet+CSP configured
- Memory stream: 9,771 memories, healthy
- Wiki: 4/5 domains at v2 compiled Apr 11
- Platform connections: 6/10 active (Spotify, Calendar, Gmail, YouTube, Whoop, Twitch)
- Twin chat: works correctly, streaming available
